RMIT Adflow - Version & Changelog History

v0.19.14 (Current) - June 2026 — Engine v2.19
- Animation Hover Previews Stop Reliably: Fixed an issue in edit mode where animation, effect, or frame-transition hover previews could keep playing after moving the mouse away from the animation panels. Previews now stop as soon as the cursor leaves the three animation sub-panels (Animation, Effects, Frame Transition), even when the panel content was re-rendered under the cursor.
- Animation Direction Preview Fixed: Fixed the entrance-animation Direction dropdown, which previously threw an error and showed no preview when hovered. Hovering the Direction control and its options now correctly previews the animation in the chosen direction.

v0.19.13 - June 2026 — Engine v2.19
- File Loading Progress Dialogue: Added a blocking modal progress dialogue that displays when opening a project from a local file, template, or cloud storage. The modal disables Escape key navigation, backdrop clicks, and has no close button, preventing user interaction during load. Displays a smooth progress bar and detailed status text (downloading, reading structure, extracting assets 1-by-1, rendering workspace).

v0.19.12 - June 2026 — Engine v2.19
- Button Zoom Animation Stagger: Added a toggle to stagger the entrance animation for button elements when their transition is set to "zoom". This configuration animates the background container/stroke immediately, followed shortly (0.15s offset) by a staggered fade-in/zoom of the button's text, providing a premium feel. The stagger setting is synchronizable across linked canvases.

v0.19.11 - June 2026 — Engine v2.19
- Template Sanitization & Preference Protection: Fixed issues where projects created from templates carried isTemplate: true metadata permanently in autosaves, cloud saves, and exported .flow packages. The runtime state now deletes this flag upon load. Stripped personal workspace preferences, favorite animations, and custom asset folders/libraries from template exports to avoid polluting user environments, and protected active user settings from being overridden when importing a template. Reset active data-merge versioning states (version pointers, locks, and sort keys) during template export and loading.
- Template Naming Convention: Implemented a dedicated template naming convention (<project-name>.template.flow) for exported template files.
- Restored Resume View Toast: Restored and corrected the "Resume previous view" toast notification on project startup and file loading, enabling users to jump back to their last saved scroll/zoom positions when opening a project (bypassed entirely for templates).

v0.19.10 - June 2026 — Engine v2.19
- Preview Current Only Disabled for Single-Frame Ads: The "Preview current only" checkbox in the top bar is now greyed out and disabled when only one playable frame exists — either a one-frame project, or a two-frame project with one frame marked Skip. If it was checked when the frame count drops to one (e.g. by skipping a frame), it is automatically unchecked, so a stale "current only" preview can't linger with the control locked.

v0.19.9 - June 2026 — Engine v2.19
- Letter Spacing Now Exports: Fixed exported ads (HTML5 ZIP, Full Preview, and PNG fallbacks) silently dropping the Letter spacing set on text and buttons — the editor rendered it but the export markup never emitted the CSS property. Exported text now matches the editor's tracking exactly, including per-character Typing/Word-fade spans (which inherit it). Because the export's runtime auto-size fitter measures the live label, it now also measures with letter spacing — so auto-sized text and buttons with tracking pick the same font size and wrap point in the editor, preview, and export (previously negative tracking could make a one-line label wrap to two lines in the shipped ad, and positive tracking could make it export at a larger font than designed).

v0.19.8 - June 2026 — Engine v2.19
- Complete Link Group Animation Sync: Fixed "Fade letters" (Typing preset) and "Fade BG" toggles not propagating across linked elements — they were missing from the IN Animation sync list in applyLinkSync. The same audit closed further sync gaps: Letter spacing and Auto line height now sync under Font settings (the list previously referenced obsolete property names that no longer exist), text background Cover % and Opacity now sync under Background, and stroke Dash / Gap / Opacity now sync under Stroke for buttons and shapes. "Reset Settings" on an animation preset now also clears the two fade toggles.
- Restored Startup Templates: Regenerated Startup/registry.json, which still pointed at template files that no longer exist — the startup templates (RMIT_Blank_2026, RMIT_Blank_Red) now load again instead of failing silently.

v0.19.7 - June 2026 — Engine v2.19
- Fixed Button Text Under Zoom: Button labels no longer grow and shift in preview and export when the button has a Zoom in-animation that starts below 100%. The auto-fit was measuring the label while the zoom still had it scaled down, so it overshot the font size; it now divides the live zoom scale back out, so the preview/export label matches the editor.

v0.19.6 - June 2026 — Engine v2.19
- Frame Control Pages: When the top bar is too narrow for every frame control, the row now splits into pages instead of scrolling — no scrollbar at all. A clickable arrow appears at whichever edge has more controls (Skip Frame, Duration, Loop, Preview-current-only); clicking it flips to the next or previous page, like changing frames.

v0.19.5 - June 2026 — Engine v2.19
- Clearer Animation Sections: The In Animations, Continuous Effect, and Frame Transition groups in the Animation panel now read as distinct cards with bolder, accent-marked headings, so they're easy to tell apart even with every setting expanded.
- Refined Account Chip: The sign-in / account button in the top bar is now a cleaner 32px circle — no ring border, with subtle hover tints (muted grey when signed out, accent-tinted when signed in).
- ClickTag Promoted in Dynamic Data: The ClickTag (exit URL) mapping is pinned to the top of the Dynamic Data mapping list and marked "Required", and its dropdown now shows the current default exit URL so it's clear what ships when no column is mapped.
- Top Bar Polish: "Full preview" and "Export" are now equal-width buttons, and the dynamic-data indicator on the version switcher is slightly larger.

v0.19.4 - June 2026 — Engine v2.19
- Scrollable Frame Controls: When the top bar runs out of room, the frame controls (frame selector, Skip Frame, Duration, Loop, Preview-current-only) now scroll horizontally instead of being cut off — with soft fade hints on the left/right edges signalling there's more to see. A vertical mouse-wheel scrolls the row when it overflows.

v0.19.3 - June 2026 — Engine v2.19
- New Projects Open Centered: When you create a new project, the whole canvas group is now centered on the workspace board rather than sitting in the top-left corner — so it opens with even space on every side for arranging canvases and temporarily parking elements.

v0.19.2 - June 2026 — Engine v2.19
- Smaller, Tidier Workspace Board: Shrank the main workspace board — the pannable area behind your canvases — from 5000×5000 down to 3000×3000, and anchored canvases near the top-left instead of the middle. Previously most of the board was empty space, so it was easy to scroll off into the void and lose sight of your work. There's now a sensible work area with room to the side and below for temporarily parking elements while you design. Existing projects are automatically re-homed onto the smaller board when opened, so nothing is lost or clipped.

v0.19.1 - June 2026 — Engine v2.19
- 1366×768 Screen Support: Lowered the minimum supported screen size from 1920×900 down to 1366×768, so the editor now opens on standard laptop displays instead of showing the "get a bigger screen" overlay. The width is a hard floor (below 1366 the interface can't lay out without clipping); the height floor is forgiving because real 1366×768 monitors only leave ~600–660px of viewport once the browser and OS chrome are accounted for.
- No Horizontal Clipping at 1366px: Reworked the three-column workspace and the topbar so everything fits at 1366px wide with nothing cut off on the right. The canvas area now shrinks to fit between the two side panels (instead of pushing the right panel off-screen), and the topbar spacing was tightened so the Preview and Export buttons stay fully reachable. Panels scroll vertically when the window is short — the layout never scrolls or clips horizontally.

v0.19.0 - June 2026 — Engine v2.19
- Continuous Animation Settings Expansion: Added scale parameters for Pulse and Heartbeat continuous effects, and range and direction options for the Float continuous effect. The settings are fully integrated with real-time viewport preview rendering, HTML/ZIP export pipelines, and link-group cross-canvas sync.

v0.18.9 - June 2026 — Engine v2.18
- Movement/Scale Effects No Longer Clipped on Buttons: Fixed continuous effects (Wiggle, Pan, Zoom, Spin, etc.) being cropped to the button's bounding box in Preview/Export when the button also had a clip-path entry animation (Swipe / Typing). The reveal/clip from the entry animation was wrapping the movement; the continuous-effect wrapper now sits outside it, so the button can move and scale past its box exactly as it does in the editor.
- Tidier Button Sizing Controls: Reorganized the button auto-size controls in the Properties panel — the Auto-size and Wrap toggles now share one row, with Size / Max / Wrap-threshold grouped on the row below. The wrap threshold ("Wrap <") only appears when it applies (Auto-size + Wrap both on), removing the cramped layout and the wrapped hint text.

v0.18.8 - June 2026 — Engine v2.18
- Per-Button Wrap Threshold: Added a "Wrap below" control to auto-sized buttons (Properties panel, alongside Size / Auto / Wrap). The button keeps its label on one line until auto-sizing would shrink the font below the set threshold; below that, it wraps to a (usually larger) multi-line layout instead. This makes automatic line-breaking useful again — previously, after the wrap-consistency fix, auto-sized buttons only broke when the text became tiny. The threshold is per-button, syncs with font settings across linked sizes, and is applied identically in the editor and the export/preview.

v0.18.7 - June 2026 — Engine v2.18
- Consistent Button Text Wrapping: Fixed auto-sized button labels wrapping to two lines in Preview/Export while staying on one line in the editor (with "Wrap" enabled). Auto-sized buttons now pick the largest font that fits the label on a single line — measured the same way, with a small safety margin, in both the editor and the export sizers. Previously the sizer could choose a font whose one-line text was a hair too wide; it rendered as one line in the editor but tipped into wrapping in the preview on displays with fractional scaling (DPR). The margin makes the result identical across editing, preview, and export on any display.

v0.18.6 - June 2026 — Engine v2.18
- Floppy Disk Save Icon: Converted the save status indicator dot next to the project name in the topbar to a minimal and sleek floppy disk SVG icon. The icon dynamically updates its stroke color and applies subtle shadow glow transitions based on active save status (Green: Saved, Purple: Saving, Amber: Unsaved, Red: Error).

v0.18.5 - June 2026 — Engine v2.18
- Dropdown Visual Indicators: Added visual indicators (▼ down chevrons) next to the labels of "Brand Elements" and "Brand Sets" buttons.
- Distinct Brand Button Styling: Styled brand-specific dropdown buttons with an RMIT accent-tinted border and a subtle background highlight to visually distinguish them from standard single-click action buttons.

v0.18.4 - June 2026 — Engine v2.18
- Add Brand Sets Button: Added a "Brand Sets" button inside the left panel "Add element" grid to match the "Brand Elements" button. Clicking it displays a dropdown selector to place pre-defined brand sets (e.g. Logo + RFWN + CRICOS) instantly onto the active canvas.
- Add to all Canvases & Auto-Arrange Checkbox: Added a checkbox "Add to all canvases and auto-arrange" to the top of the left panel "Add element" section. When checked, adding any element, brand element, or brand set automatically creates a copy across all canvases in the workspace and auto-arranges them to fit the dimensions.

v0.18.3 - June 2026 — Engine v2.18
- Blur Frame Transition: Added a customizable "Blur" frame transition preset that creates a cross-blur transition, complete with adjustable "Blur Amount" (0-100px) and "Scale Blend" (10-500%) controls to configure defocus strength and continuous camera zoom-in/out effects (with optional fade).
- Overhauled Splash Loading Screen: Created a more dynamic yet sleek loading experience featuring organic shifting ambient background glows, a breathing drop-shadow pulse on the brand logo, smooth easeOutExpo slide-fades on status updates, and a glassmorphic gradient progress bar. Exit transition now executes a soft camera lens defocus zoom-out.
- Outline Mode Enhancements: Added smart color-coding in both light and dark outline modes. Elements bound to dynamic data merge columns now draw with an Amber outline, elements with intro animations or continuous effects draw in Pink, and elements featuring both draw in Purple, making layer properties readable at a glance in wireframe mode.



v0.18.2 - June 2026 — Engine v2.18
- Frame Transitions UI & Presets: Renamed "Transition" sidebar heading to "FRAME TRANSITION". Added "Short edge" and "Long edge" dynamic direction choices to Slide, Push, and Swipe transitions. Upgraded the Split transition to remove the raw Angle field, replacing it with the same Direction select dropdown.

v0.18.1 - June 2026 — Engine v2.18
- Closest Edge Slide Preset: Added "Closest edge" animation direction to the Slide transition preset. When selected, the animation direction (Up, Down, Left, or Right) is determined dynamically per canvas, checking which edge of the parent canvas is closest to the element's center coordinate. This ensures shared link-group elements or elements of different sizes always slide in from their closest canvas boundary.

v0.18.0 - June 2026 — Engine v2.18
- Outline Mode: Added Adobe Illustrator-style Outline Mode (toggled via View -> Outline Mode or Ctrl+Y) to render layouts as 1px vector wireframes, hide solid background/shape fills, display raster images as bounding boxes with crossed diagonal lines, and draw text contours.
- Redo Shortcut Relocation: Relocated the default Redo keyboard shortcut from Ctrl+Y to Ctrl+Shift+Z to accommodate the new Outline Mode shortcut.

v0.17.9 - June 2026 — Engine v2.18
- Data Merge Scrollbar Optimization: Eliminated double scrollbars in the Data & Versions modal by making the modal body and panels flexbox-driven and changing the sheetTable container style to flex: 1. All overflow scrolling is now strictly confined to the versions list table.

v0.17.8 - June 2026 — Engine v2.18
- Sticky Header Highlight Fix: Resolved overlap issues during mid-scroll column highlights in the Data & Versions modal by making the highlighted header background opaque (mixing accent with the panel background instead of transparency).

v0.17.7 - June 2026 — Engine v2.18
- Sticky Table Headers: Configured the Data & Versions sheet headers to remain sticky at the top when scrolling down long list versions, using a solid backdrop color matching the active theme.

v0.17.6 - June 2026 — Engine v2.18
- Data Merge Dropdown Fix: Resolved click registering issues inside the Data & Versions mapping dropdown by locally updating the select components to render selection updates instantly without resetting the scroll position of the panel.
- Background Validator Asset Audit: Updated compliance checking routines to audit active spreadsheet version assets instead of falling back to default template values, eliminating mismatched compliance warning banners.
- Dynamic Slot Preservation: Configured Distributed/Auto-Resized elements to retain active spreadsheet mapping assignments instead of resetting to none.

v0.17.5 - June 2026 — Engine v2.18
- Exclude Persistent Layers Option: Added a checkbox in the transition panel to exclude persistent layers from transitions, allowing them to remain static on top/bottom while other elements transition.

v0.17.4 - June 2026 — Engine v2.18
- Looping Frame Transitions: Allowed the first frame to have transition settings and preview animation when looping is enabled, mapping it correctly to the export pipeline.

v0.17.3 - June 2026 — Engine v2.18
- Button Renaming: Renamed "Batch WebP Compress" to "Batch WebP Compression" for UI consistency.
- Deflate Zip Sizing: Configured size calculation functions to measure the exact DEFLATE-compressed ZIP size instead of raw UNCOMPRESSED (STORE) size. This resolves false-positive oversize flags and matches actual display ad network limits.

v0.17.2 - June 2026 — Engine v2.18
- RMIT Logo Recovery: Implemented a self-healing repair pass that automatically restores any previously compressed/rasterized RMIT brand logo back to its original clean vector SVG format, and strengthened the SVG bypass logic during auto-compression to filter logo/brand elements based on name and role.

v0.17.1 - June 2026 — Engine v2.18
- Batch WebP Compression: Added a green "Batch WebP Compress" button in the Export modal to compress all oversized canvases and versions to WebP format concurrently, displaying a detailed progress loading bar and cancellation capability.
- Dynamic Sizing Sync: Configured both the Ads Validator details modal and the Export modal table to dynamically recalculate exact ZIP sizes concurrently, ensuring sizing values are always up-to-date and consistent.
- SVG Bypass Check: Bypassed SVG vector images during WebP auto-compression to preserve branding logos (such as the RMIT logo) from being rasterized or cropped.
- Validator Header Cleanup: Removed the redundant, top-aligned green "Auto Compress (WebP)" button from the Ads Validator header.

v0.17.0 - June 2026 — Engine v2.18
- Paste in Place Shortcut (Ctrl+Shift+V / Cmd+Shift+V): Added paste-in-place keyboard functionality. Keeps element coordinates exact when pasting on the same canvas, and dynamically scales positioning proportionally when pasting onto target canvases of different dimensions using center-anchored coordinate scaling to preserve centered objects exactly.
- Updated Keyboard Shortcuts Modal: Registered Shift + Ctrl/Cmd + V inside the Help -> Shortcuts dashboard for discoverability.

v0.16.94 - June 2026 — Engine v2.18
- Frame Sync Dialog Renaming: Renamed the main Synchronize Layers dialog header to "Frame Sync" to align with its core context.

v0.16.93 - June 2026 — Engine v2.18
- Nested Frame Sync Submenu: Moved the canvas and frame layer sync action triggers into a clean, nested "Frame Sync" right-click context submenu, keeping the main canvas viewport menu clean.

v0.16.92 - June 2026 — Engine v2.18
- Synchronize Layers Context Actions: Added direct "Sync Across Canvases..." and "Sync Across Frames..." options to the canvas right-click context menu, pre-selecting the respective tab on open.
- Refactored Sync Tab Labels: Renamed "Canvas Sync" to "Sync Across Canvases" and "Frame Sync" to "Sync Across Frames" to clearly describe their functionality.

v0.16.91 - June 2026 — Engine v2.18
- Static Clear Recent Button: Configured the "Clear Recent" File dropdown menu item to remain permanently visible instead of dynamically hiding/showing after project list loading delays.

v0.16.90 - June 2026 — Engine v2.18
- Client-Side Recent Cloud Filtering: Fixed Clear Recent behavior for cloud projects. Instead of deleting physical project database rows, clearing recent cloud projects now saves a client-side timestamp in localStorage (`cloud-recents-cleared-at`) and filters the visible menu items.

v0.16.89 - June 2026 — Engine v2.18
- Repositioned Clear Recent Menu Item: Moved the "Clear Recent" menu item out of the sliding Open Recent submenu and placed it directly under "Open Recent" in the main File dropdown list, preventing unnecessary hover-scrolling for users.

v0.16.88 - June 2026 — Engine v2.18
- Open Recent Visual Sort & Clear Action: Added a "Clear Recent" option in the slide-out Open Recent menu. Triggering this clears the recents list, keeping only the single latest file for both local and cloud categories. The Local and Cloud section groups are now ordered dynamically, ensuring that whichever category holds the most recently modified project appears on top.

v0.16.87 - June 2026 — Engine v2.18
- Isolated Link Sync Options in Frame Sync Dialog: Separated the "Break Link Group" checkbox from general styling "Sync Options" into its own dedicated "Link Sync Options" section with a horizontal divider, improving dialog usability and design clarity.

v0.16.86 - June 2026 — Engine v2.18
- Frame Sync Break Link Group: Added a "Break Link Group" sync option checkbox to the Frame Sync tab in the Synchronize Layers dialog. When enabled, link group identifiers are removed from cloned layers so that duplicated contents across different frames edit independently.

v0.16.85 - June 2026 — Engine v2.18
- Button Visual Polish: Removed decorative bolt icon (⚡) from WebP Auto-Compression action buttons in the validator to match RMIT's clean, minimalist UI design system.

v0.16.84 - June 2026 — Engine v2.18
- Validator Version Switcher: Added a version selector dropdown to the Ads Validator details modal header (displayed when Data Merge spreadsheet rows exist), permitting live auditing of different dynamic versions directly from the validation report.
- Fixed WebP Compression Error: Resolved the "Undefined" error during auto-compression by rejecting the loading promise with a proper Error object instead of a generic Event.

v0.16.83 - June 2026 — Engine v2.18
- Button Visual Polish: Removed decorative emoji/unicode icons (⚡ and 🔄) from "All Versions Validator" and "Re-run" buttons to align with RMIT's clean, minimalist UI design system.

v0.16.82 - June 2026 — Engine v2.18
- Cached Batch Audit Results: opening the "All Versions Validator" when a previous batch run exists now displays the cached result instantly.
- Added a "Re-run" action button to both the success screen and the issues listing screen of the Batch Validator results pop-up, allowing instant refresh when templates or elements change.

v0.16.81 - June 2026 — Engine v2.18
- Added an "All Versions Validator" button to the export dashboard when multiple spreadsheet data merge rows exist.
- Implemented asynchronous batch auditing across all versions and canvases, displaying a progress overlay bar to prevent browser thread freeze.
- Designed a premium batch audit results pop-up that lists all detected issues with click-to-fix shortcuts that load the faulty version/canvas directly into the editor and launch the validator.

v0.16.80 - June 2026 — Engine v2.18
- Persisted Export Dashboard: opening the Ads Validator modal via the export dashboard now keeps the export dashboard open underneath.
- Top-most Modal Escape Support: updated the Escape key event handler to only close the active (topmost) dialog when multiple modals are stacked.
- Narrowed the Click Tag column on the export dashboard table (width 180px) to maximize compliance status badge visibility.

v0.16.79 - June 2026 — Engine v2.18
- Expanded Export Dashboard: Added columns for "Ad Compliance", "Accessibility", and "Branding" validation status.
- Added event handlers so clicking a validation status badge in the export dashboard closes the dashboard and opens the relevant tab in the Ads Validator modal.
- Integrated an "Ads Validator" action button into the bottom left of the export dashboard.

v0.16.78 - June 2026 — Engine v2.18
- Finalized component renaming: updated the validator to be called "Ads Validator" instead of "Validation Dashboard".

v0.16.77 - June 2026 — Engine v2.18
- Renamed the "Ad Validator" / "validator dashboard" component to "Validation Dashboard" across all tooltips, sidebar controls, settings inputs, and documentation to reflect its expanded scope (compliance, branding, and accessibility).

v0.16.76 - May 2026 — Engine v2.18
- Added a helpful toast notification ("Drag and draw a box to add text") when a user clicks on the canvas instead of dragging to draw a text box.

v0.16.75 - May 2026 — Engine v2.18
- Disabled click-to-add default size text boxes for the Text Tool, enforcing a drag gesture of at least 5px to spawn a text element.

v0.16.74 - May 2026 — Engine v2.18
- Fixed Text Tool double-creation bug: clicking on an empty canvas area or workspace while editing a text element now blurs and commits the active edit without spawning a new text element or starting a marquee selection.

v0.16.73 - May 2026 — Engine v2.18
- Removed the continuous pulsing animation on the save status indicators.
- Transitioned indicator status changes to use a simple CSS color/background/border fade transition.

v0.16.72 - May 2026 — Engine v2.18
- Configured dynamic status colors for the topbar save badge mapping to user specs: "Save locally" (Accent), "Cloud synced" (Blue), "Saved & synced" (Green), and "Unsaved" (Amber).

v0.16.71 - May 2026 — Engine v2.18
- Customized save status text strings according to user design specs ("Save locally", "Cloud synced", "Saved & synced", and "Unsaved").
- Increased the fixed width of the save status container to 96px in CSS to ensure larger statuses fit cleanly.

v0.16.70 - May 2026 — Engine v2.18
- Configured a fixed width of 90px on the save status container and centered text layout to prevent topbar content shifting.
- Introduced the "Unsaved Local" amber-alert badge state, warning users when changes are synced in the cloud but not yet auto-saved locally.

v0.16.69 - May 2026 — Engine v2.18
- Replaced the dual local/cloud save status checkmark icons in the top bar with a single dynamic, HSL-themed text badge ("Unsaved", "Saving...", "Saved", "Syncing...", "Saved + Cloud", "Save Error", "Sync Error").
- Optimized layout width dynamics of the save status container and designed animated state transitions (pulses and shakes) for premium glassmorphic visual feedback.

v0.16.68 - May 2026 — Engine v2.18
- Resolved image alignment drift and relative motion bugs under vector mask continuous animations.
- Bound both the parent mask container and child image layer to a shared, dynamic transform-origin aligned to the mask's center coordinate.
- Corrected child image translation offsets to compensate for base rotation layers, keeping the image stationary inside its moving mask.

v0.16.67 - May 2026 — Engine v2.18
- Fixed continuous FX animation rendering for vector masks under the CSS clip-path system.
- Implemented mask container movement matching the chosen continuous effect (e.g. spin, wiggle, pulse).
- Added automatic counter-animations on the masked image inside the wrapper to keep the background stationary while the mask moves.
- Exported mask continuous effect parameters, variables, and inverted keyframes inside HTML and ZIP deliverables.

v0.16.66 - May 2026 — Engine v2.18
- Added dynamic Canvas Zoom & Select Tool controls inside the main canvas footer.
- Tied workspace keyboard shortcut keys (V for Select tool, Z for Zoom tool) for intuitive editing mode switches.
- Added mouse panning support and Alt key toggle options to invert standard zooming directions when magnifying.
- Enforced strict fullscreen preview mode safeguards to prevent tool switching and disable click-to-zoom actions.

v0.16.65 - May 2026 — Engine v2.18
- Added dynamic scanning of the `Startup/` templates directory, automatically parsing flow archives to register pre-defined templates in a generated registry manifest.
- Refactored the Settings panel and the New Project wizard to display dynamic template options instead of hardcoded startup preferences.
- Preserved user-defined Project Names when spawning a new workspace from a custom startup template instead of overwriting state metadata.
- Corrected layout version headers in settings modals and dialogs to match current release version (v0.16.65).

v0.16.64 - May 2026 — Engine v2.17
- Implemented dynamic right-side boundary clamping (maxRight) for Heading and Subheading elements on 970x250 canvases (Billboard) in both Auto-Arrange and Auto-Resize to prevent overlaps with right-half elements and CTA buttons.
- Revamped the Auto-Resize execution dialogue and Settings modals, removing obsolete properties (e.g. main image cover fallback).
- Added behavior settings and checkboxes to toggle subheading visibility on 320x50 canvases (hideSubheading320x50) and automatically lock brand elements (lockBrandElements, covering Logo, Tagline, and CRICOS layers) after Auto-Resize and Auto-Arrange.
- Updated the canvas right-click "Auto-Resize" menu action to always display the execution dialogue instead of executing instantly.
- Always add target elements to their respective link groups during Auto-Resize, but set the group's liveLink property to false (disabled) by default unless live-linking is enabled in the settings.
- Added a "Live Linking" toggle to the right-click selection context menu for grouped elements to enable/disable real-time style propagation on the fly.
- Updated placeCtaButton to detect and respect the horizontal alignment of the CTA button on the source canvas (Left, Center, Right) when placing it in vertically stacked layouts (300x600, 160x600, and default fallbacks).

v0.16.63 - May 2026 — Engine v2.16
- Renamed the validator specifications tab to "Ad Compliance" and updated its layout and status icons.
- Refactored validation status badge indicators in the editor sidebar and modal list to support three states: green check (✓) only when all checks pass, orange warning (⚠️) when warnings exist but no blocker errors exist, and red warning (⚠️) if any critical Ad Compliance blockers are tripped.
- Made the Accessibility and Branding audit engine evaluation synchronous and real-time, executing automatically inside the main render() loop on every canvas edit.
- Placed interactive input event listeners on frame transition numbers to propagate edits instantly to the validation checks.
- Bypassed touch target checks for canvases configured as a full-screen click area (c.fullClickArea !== false).
- Customised the brand colors warning message to state that the color is "in proximity of brand color, so use exact brand color (#E61E2A or #000054)".

v0.16.62 - May 2026 — Engine v2.16
- Relocated all transition settings from the top-bar controls to a dedicated visual sub-section in the sidebar Animation panel (positioned below Continuous FX).
- Added bookmarking/favoriting support for frame-level transitions via right-click, fully integrated with the global favorites filter.
- Removed horizontal line separators between the three Animation sub-panels, styling each sub-panel with a subtle card background and optimized spacing to eliminate wasted room.
- Added the "Push" viewport-wide panning frame transition supporting custom directions, fade, and elastic spring bounce.
- Added the "Iris" focal expansion frame transition supporting Circle, Square, and Diamond shapes expanding from Center or any corner with optional fade.
- Added the "Zoom" frame transition supporting custom starting scale (Zoom From %), opacity fade toggle, and multi-step elastic spring bounce physics.
- Added the "Split" frame transition supporting diagonal reveals along customizable angles and fade toggling.
- Refined the loop preview triggers for all three animation panels (Entrance Transitions, Continuous FX, and Frame Transitions) to disable container-wide hover triggers, launching previews only when hovering over presets or interacting with settings/inputs, and updating settings instantly.

v0.16.61 - May 2026 — Engine v2.16
- Fixed canvas selection inside mask groups: enables selecting the underlying image directly on the canvas (even when clipped or larger than the mask) via group isolation mode.
- Implemented backdrop click hit-testing for isolated groups to capture clicks on clipped image bounds that pass through due to CSS clip-path pointer event suppression.

v0.16.60 - May 2026 — Engine v2.16
- Implemented high-performance version export using a background Web Worker and direct-to-disk streaming.
- Developed a lightweight, zero-dependency client-side ZIP stream writer that pipes chunks directly to disk via the File System Access API.
- Integrated Web Worker-driven sub-zip compression to ensure the main UI thread remains completely responsive during bulk version generation.
- Added a beautiful, responsive overlay progress modal displaying real-time version progress, percentage completed, and cumulative MB written, along with a "Cancel" option.
- Added a unified memory buffer fallback for Firefox and Safari to ensure seamless version exports even on unsupported browsers.
- Respects selected canvases and filename prefix options from the Export dialog during bulk exports.
- Fixed "Crop & Level" and "Compress" image tools to load the active version's image, saving outputs back to the data sheet cell if dynamic.

v0.16.59 - May 2026 — Engine v2.16
- Separated manual saving into three distinct options:
  1. Ctrl+S to save silently to the Supabase Cloud.
  2. Ctrl+Shift+S to save silently to the browser's IndexedDB local storage.
  3. Menu option "Save to File (.flow)" (with no keyboard shortcut) to trigger a `.flow` file download.

v0.16.58 - May 2026 — Engine v2.16
- Updated the default IndexedDB auto-save interval from 1s to 10s.
- Added a new custom auto-save interval selector under the "History & Saving" section in Settings, letting users configure debounced auto-saves from 5s to 60s (1m).

v0.16.57 - May 2026 — Engine v2.16
- Added a comprehensive Frequently Asked Questions (FAQ) section to the main project README.md, outlining Quick Workflows, Data Merges, Progress Saving, and other typical usage troubleshooting steps.
- Introduced a detailed "Powerful Features" list under the Getting Started category in both the repository README.md and the in-app Help menu, summarizing the core value propositions and system capabilities.

v0.16.56 - May 2026 — Engine v2.16
- Added a dedicated "FAQ" section to the in-app Help documentation modal, featuring detailed guides on Quick Workflows, Data Merges, Progress Saving/Autosaves, Animation troubleshooting, Unlinking elements, Ad Weight optimization, and Offline Usage.

v0.16.55 - May 2026 — Engine v2.16
- Expanded all in-app Help category introduction pages with much more detailed descriptions, and refactored the "Technical Detail" footers into user-friendly "General Tips" sections.

v0.16.54 - May 2026 — Engine v2.16
- Added structured "Introduction" sub-pages for all in-app Help documentation sections, describing what each section does, what Adflow offers, how it excels, and their specific technical underpinnings (styled in a faint, smaller font).

v0.16.53 - May 2026 — Engine v2.16
- Increased the Adflow brand logo size (max-width scaled from 140px to 280px) on the "Welcome to Adflow" home tab of the in-app Help documentation modal.

v0.16.52 - May 2026 — Engine v2.16
- Added the Technical Stack guide to the end of the in-app Help documentation modal, split into dedicated tabs: Architecture & Sandbox, Global State Schema, Auto-Resize Engine, Masking & Link Sync, and Persistence & Cloud Security.

v0.16.51 - May 2026 — Engine v2.16
- Cleaned up documentation titles by removing emojis and icons from headers in the README and the Help system menu.
- Added a detailed "Technical Stack" guide section in the README, detailing local storage structure, global state typing definitions, dynamic data merge architectures, and Supabase RLS recursion helper functions.

v0.16.50 - May 2026 — Engine v2.16
- Mask system revamp: replaced SVG `<mask>` + CSS `mask: url(#…)` with CSS `clip-path` using inline shape functions. The previous system relied on the browser resolving a CSS `url(#fragment)` reference against an inline SVG `<mask>` element — the most brittle paint operation available, with reproducible per-browser failures (Chromium nested-defs scope, Safari zero-size-SVG paint context, Firefox shorthand-not-propagating-to-mask-image, etc). Every report of "mask + image invisible on another browser/machine" was traced to that one fragment-URL resolution path.
- New approach: `clip-path` with inline shapes — no SVG defs, no fragment URL, no per-browser quirks. Maps Adflow's three mask types directly:
    • `rect` (optionally rounded) → `clip-path: inset(top right bottom left round radius)`. Rotated rect falls back to `polygon()` with 4 rotated corners (drops rounded corners, which polygons can't express — rotated rounded rects are an unusual mask anyway).
    • `circle` / ellipse → `clip-path: ellipse(rx ry at cx cy)`. Rotated non-circular ellipse falls back to a 36-point `polygon()`. Rotated circle stays as `ellipse()` (rotation has no visual effect on a circle).
    • `pixel` (custom brand shape) → `clip-path: path("M…")` with the source path transformed into the image's local coord space (scale + translate baked into each coordinate). Rotated pixel walks the source in absolute coords, rotates each point, and emits absolute `L`/`C` commands.
- Same data model end-to-end: still `isMask: true` + shape geometry + image-below pairing. Saved `.flow` files don't need any migration — they just render correctly now on every browser, not only the one that saved them.
- Code shrink: `elementNode`'s mask block goes from ~50 lines (build SVG defs + maskShape XML + CSS mask URL) to 8 lines (compute clip-path + apply CSS). `export-pipeline.js` mirrors the same shrink. The shared logic lives in new helpers `buildMaskClipPath()` + `_buildPixelClipPath()` + `_maskRotPt()` near other rendering helpers.
- Trade-offs the new system accepts: (1) the hover-preview mask animation (entry/effect anims applied to the `<g class="mask-g-entry">` inside the SVG) no longer fires — `clip-path` can't be animated through inline shape functions the way SVG `<mask>` children could. Animations on the masked IMAGE itself still work. (2) `clip-path: path()` browser support: Chrome 88+, Firefox 63+, Safari 13.1+ (all shipped 4+ years ago). Pre-13 Safari falls back to no clip (the image renders un-clipped, which is at least visible — pre-fix it was invisible).
- Symptom recap: mask + image both appeared invisible (mask body is `visibility:hidden` by design; image was fully clipped by an unresolvable mask URL). The only workaround was removing "Use as mask". v0.16.50 makes the clip resolve correctly regardless of which browser opens the file.
- Mid-fix bug caught: the pixel `path()` clip uses SINGLE quotes around the path data (`path('M…')`), not double quotes. The export HTML embeds the clip-path value inside an HTML `style="…"` attribute, and a double-quoted path string closes the attribute prematurely at the first `"` after `path(`, leaving the clip-path silently inactive in preview / export (image rendered un-clipped). Editor preview wasn't affected because it uses `style.setProperty` (JS-side, no attribute boundary). Switched to single quotes — both editor and export now apply the clip correctly.

v0.16.49 - May 2026 — Engine v2.16
- Mask shapes can now join link groups. Pre-fix the Link panel showed a "Mask layer — link groups disabled" notice whenever the selection included any element with `isMask: true`, blocking mask shapes from cross-canvas linking. The constraint was overly defensive: mask geometry on auto-resize is handled by the engine's mask post-pass (which positions mask relative to its image via `below.width / srcImg.width`), and that pass is independent from any link-group sync. Removed the gate at script.js:4982 so mask shapes go through the normal same-category link-group UI; you can now sync mask fill / stroke / radius across canvases like any other shape.

v0.16.48 - May 2026 — Engine v2.16
- Data & Versions modal: added Cancel button next to Save. Cancel snapshots `state.dataMerge` when the modal opens and restores it on click, discarding every cell edit / column rename / mapping change / row reorder that happened during the session. A single `pushHistory()` follows the restore so Cancel is a discrete undoable step (Cmd-Z right after Cancel undoes the discard). Save behaves as before — close and keep edits. ESC and outside-click intentionally still behave like Save (close-and-keep), so Cancel can't fire by accident; it must be an explicit click.

v0.16.47 - May 2026 — Engine v2.16
- Text elements no longer break words mid-letter. Pre-fix every text-rendering path (editor span, editor edit-mode `.editable`, measureDiv used by auto-size, multi-line bg span, and HTML5 export span) hard-coded `word-break: break-word`, which split long words like "Interactivity" into "Interactiv\ny" when the container was narrower than the word. New default is `word-break: normal; overflow-wrap: normal` everywhere, so words stay whole and the auto-size shrink logic handles fit instead. If a single word genuinely can't fit at the minimum auto-size, it overflows — but that's a clearer signal to the designer (resize the box, shorten the copy, drop the font floor) than a silently mid-word-broken line. Applies to both editor preview and exported HTML.

v0.16.46 - May 2026 — Engine v2.16
- Version cycle buttons (‹ / › next to the version dropdown) now skip the "No version" slot. Pre-fix the cycle was `null → 0 → 1 → … → L−1 → null → 0`, which made the buttons feel unresponsive: hitting Next on the last row landed on "No version" (canvas reverted to template defaults, looking like the click had been swallowed) and a second click was needed to wrap to row 0. Same issue going Prev from row 0. New behaviour is a pure `0 … L−1` wrap, so every click visibly advances to a real row. The "No version" state is still reachable via the dropdown — only the cycle buttons skip it.

v0.16.45 - May 2026 — Engine v2.16
- Text link-group "Background" sync now also propagates the text-bg padding (`bgPadL` / `bgPadV` — Properties panel "L/R Pad" + "T/B Pad" fields) along with the bg colour, hasBg toggle, animate toggle, and time offset that were already in the sync list. Editing padding on one linked text element now updates every other element in the group, matching the user's mental model that "Background" covers the bg shape's full appearance including its inset around the text.

v0.16.44 - May 2026 — Engine v2.16
- Auto-Resize engine v2.16: "Fixed shape" role (formerly "Main image") now carries source-canvas cropping over to small target canvases. Pre-v2.16 the engine always contain-fitted the element into the slot between text and CTA, so a pixel-shape that bled off the right of the 300×250 source would shrink into a tiny thumbnail on 160×600 / 728×90 / 320×50 — losing the intended visual prominence. v2.16 detects when the source element extends past any source canvas edge AND the target is "small" (at least one dim smaller than source's), and instead sizes the element by `srcDim × sqrt((target area)/(source area))` — positioned at the source's normalized centre. The element comes out at roughly the same fraction of the target canvas as on source, with the per-side overflow carrying across.
- "Big" targets — both dims ≥ source's, so 970×250 and 300×600 from a 300×250 source — ignore the source cropping and fall through to the v2.15 contain path, displaying the element fully inside the canvas (no overflow). This matches the design intent: bigger canvas means more room, so the element is shown in full instead of mimicking the source's overflow.
- Slot edges that abut a text or CTA neighbour still clamp the area-scaled element so it doesn't dip into copy / button territory; the OTHER edges (against the canvas perimeter) can freely overshoot, which is the cropping behaviour we're preserving. Stack-mode (tall canvases) clamps the top against the text-bottom and the bottom against the CTA-top; wide-mode banner clamps the left against the text-right and the right against the CTA-left.
- Background-image (free-aspect) was already preserving cropping correctly through its proportional `norm.x/y/w/h × target` rule, since W and H scale independently with the target canvas — no rule change needed.
- New `enforceHeadingSubheadAdjacency` post-pass: when heading + subheading land side-by-side on the target (e.g. the h ≤ 100 wide-banner case where subheading anchors to heading.right + 8), any other placed element whose bounding box overlaps the gap zone between them is shrunk clear (via the same `_shrinkToClear` shrink-from-axis-of-greatest-divergence used by the no-touch collision pass). The placement rules already structurally satisfied this — main-image's slot starts at max(heading.right, subheading.right), CTA goes top/right-center, logo top-right etc. — but the pass enforces the rule defensively against future rule changes / misc-role intrusion.

v0.16.43 - May 2026 — Engine v2.15
- "Apply to all canvases" checkbox replaced by two scope checkboxes next to the BG colour swatch: "Per frame" and "Per canvas". Both default OFF — picking a colour broadcasts to every frame on every canvas (the same global behaviour as before, just without a separate "apply to all" toggle).
- Tick "Per frame" to confine future bg edits to the current frame only (other frames keep their colour). Tick "Per canvas" to confine future edits to this canvas size only (other sizes keep their colour). The two scope flags compose — both ON edits exactly one frame on one canvas; only "Per frame" ON edits the current frame on every canvas; only "Per canvas" ON edits every frame on this canvas; both OFF edits everything.
- Toggling either flag OFF auto-unifies on that axis: turning "Per frame" off propagates the currently-visible colour to every frame on this canvas (and clears stale per-frame overrides); turning "Per canvas" off propagates it to every canvas in the project. Lets you collapse a per-frame / per-canvas story back to a single colour without having to re-pick.
- Hex text input next to the swatch is hidden — colour is set through the picker only, since with two scope flags + a typed hex field the row got crowded. The hex input element stays in the DOM (the picker writes through it), just visually hidden.
- Data model: existing `c.bgColor` stays as the canvas-level fallback; new `c.bgByFrame[frameId]` map stores per-frame overrides only when "Per frame" is ticked during a write. With "Per frame" OFF, writes clear `c.bgByFrame` so per-frame data never lingers stale under a "global" colour.
- Read paths follow per-frame overrides everywhere: editor canvas surface shows the active frame's bg, preview iframes use the first non-skipped frame's bg as fallback, PNG export captures the active frame, and HTML5 export now paints each frame's bg on the frame div itself so animated frame transitions correctly show bg changes between frames.

v0.16.42 - May 2026 — Engine v2.15
- The "Main image" auto-resize role is renamed to "Fixed shape". The role ID stays `main-image` under the hood so existing saved projects keep working without migration — only the display label changes (Layers panel role chip, Role picker dropdown).
- New contract for the role: it is now STRICTLY aspect-preserved through any auto-resize, no exceptions. Cover-fallback is removed from placeMainImage entirely. On a 728×90 target with a 100×100 square source, the result is e.g. 80×80 (contain) rather than 80×80 in slot but with cover sometimes producing a wide-strip-crop. Trade-off: image is smaller on extreme aspects, but its proportions are guaranteed exact.
- No-drop floor (keeps a tiny minimum visible) now scales UNIFORMLY (single multiplier applied to both dims) instead of independently bumping width and height with Math.max — pre-v2.15 the floor would break aspect on non-square sources whenever it kicked in.
- Mask post-pass switched from per-axis relative scaling (relW = srcMask.w / srcImg.w, relH = srcMask.h / srcImg.h, applied independently to the target image's width and height) to a single uniform `below.width / srcImg.width` scale factor. Mathematically equivalent under the v2.14 "preserve image aspect on masked images" guarantee, but explicit so rounding drift between two separate ratio computations can't accidentally introduce mask-shape stretch. End result: when a mask group is heavily cropped in the source (small mask exposing a small region of a large image), the mask shape on every target canvas is now exact-aspect-preserved.

v0.16.41 - May 2026 — Engine v2.14
- "Pixel Shape" added from the Brand Elements popup (left panel "+ Brand Elements" button) or from the canvas right-click "Brand Elements → Pixel Shape" submenu is now registered with `role: 'main-image'` and `roleAuto: false`. Previously it was added as a plain pixel via `addElement('pixel')` and got auto-classified as 'misc' (or 'background-image' if large), which meant auto-resize didn't treat it as the hero element on target canvases. customName is set to "RMIT Pixel" for clarity in the Layers panel.
- Both entry points (Brand Elements popup at bottom of left panel + canvas context-menu "Brand Elements") now route through `addBrandElement('pixel')` for consistency.
- Belt-and-braces: autoAssignRole gets a new `el.type === 'pixel' && name.includes('pixel')` check that returns 'main-image', positioned BEFORE the generic `name.includes('rmit')` rule. If a user ever resets the brand-pixel's role to auto, it'll still be re-detected as main-image (instead of falling through to 'rmit-logo' because the name contains "rmit"). Generic pixels from "Add Element → Pixel" are unaffected — their customName is empty so the new check doesn't fire.

v0.16.40 - May 2026 — Engine v2.14
- Fix: critical regression from v0.16.39. The comment block I added inside `generateExportHTML`'s embedded CSS contained backticks around `c.bgColor` — but that CSS is itself a JavaScript template literal (wrapped in backticks), so my inner backticks broke out of the template literal and turned the rest of the function into a JS parse error. Result: `export-pipeline.js` failed to load on app boot, `generateExportHTML` was undefined, both single-preview and full-preview canvases threw `ReferenceError` and rendered as a black workspace.
- Replaced the offending backticks with plain quotes. `node -c` clean. Verified: full preview now renders all canvas frames + iframes correctly. The v0.16.39 fixes (transparent canvas, GPU compositing, clip-path, transparent body in export) are all intact — only the comment is rewritten.

v0.16.39 - May 2026 — Engine v2.14
- Canvas-bg hairline leak fixed. The thin coloured line visible around the canvas in full preview (and at non-100% zoom in single preview / browser HTML5 preview) was the canvas div's own background colour leaking through a 1–2px sub-pixel gap between the canvas div and the iframe inside it. Both were trying to paint the same `c.bgColor`, but browsers round the iframe's 100% width/height against the canvas div's pixel-explicit width/height differently under zoom — leaving a hairline strip on one or two sides.
- Three defensive changes in `previewFrameNode` and the single-preview branch of `canvasFrameNode`:
  • Canvas div background set to `transparent`. Now the iframe is the only thing painting `c.bgColor`, so there's no double-layer that can mismatch.
  • Iframe now uses explicit pixel dims (`width: c.width + 'px'`, `height: c.height + 'px'`) instead of `100%`. Pixel-equal dims match the canvas div under any zoom factor.
  • Canvas div gets `transform: translateZ(0)` (forces a GPU compositing layer with stricter sub-pixel rendering) and `clip-path: inset(0)` (precise edge clipping that overflow:hidden sometimes misses).
- No effect on PNG export — that's rasterized at exact pixel dimensions and never showed the line. No effect on layout maths anywhere.

v0.16.38 - May 2026 — Engine v2.14
- Auto-Resize engine v2.14: mask groups no longer stretch on resize. This was the actual root cause of the "thin red line" that varied across previews — the masked image was being placed with cover-fallback, which changed its aspect ratio; the mask post-pass then resized the mask shape to match the image's new bounds, stretching the mask geometry past the canvas edge. At non-100% zoom the canvas's overflow:hidden clipping was leaky and exposed the overhang as a red strip. PNG export rasterizes pixel-perfectly so it didn't show there.
- Two surgical fixes:
  • `placeMainImage` skips the cover-fallback when the source image has a mask shape above it (`findMaskAbove`). Masked images stay in pure contain-mode so their aspect ratio matches the source exactly, leaving the mask post-pass nothing to stretch against.
  • Mask post-pass switches from "make mask exactly cover the image" to "scale the mask using RELATIVE source geometry" — `mask.x/y/w/h` are computed from the source mask's normalized position within the source image (relX, relY, relW, relH all 0..1), applied against the target image's bounds. The mask group becomes a self-similar composition through any resize.

v0.16.37 - May 2026 — Engine v2.13
- Auto-Resize engine v2.13: main image aspect ratio is now preserved unconditionally on thin banners. The v2.10 size floor and the v2.8 thin-banner cover-threshold (0.9 for canvasAspect > 3) are both removed — they were producing the wide-strip-crop look on 728×90 and 320×50 that read as "stretched" when the image's actual visible portion was cropped from a much larger size. Cover-fallback now only fires on normal-aspect canvases (canvasAspect ≤ 3) with the v2.8 threshold of 0.6 fillFrac; thin banners stay in contain-mode regardless of fill percentage.
- v2.12 slot-collapsed recovery softened: when the heading + CTA pair eats the safezone width and `slot.w` ≤ 0, the fallback is now a centered SQUARE sized by the smaller safezone dim (rather than the full safezone, which combined with cover gave the wide-strip-crop). The image fits the square cleanly at natural aspect — no cropping, no stretching. Trade-off: image is smaller on extreme aspects (e.g. ~80×80 on 728×90), but its proportions stay correct, which is what the user explicitly asked for.

v0.16.36 - May 2026 — Engine v2.12
- New image utility: Crop & Level. Lives next to "Compress" in the image element's Properties panel. Opens a dialogue with a draggable crop rectangle (corners + body) overlaid on a live-rendered preview, plus a -180°/+180° rotation slider with numeric input + Reset. The rotation is baked INTO the cropped image — the element's own rotation property stays at 0, exactly so a quick horizon-level doesn't dirty the rotation handle. Successive edits restart from the saved original (not the already-cropped result), so resolution doesn't degrade across re-crops. A "Restore original" button drops the crop entirely.
- Implementation: el.cropOriginalAssetId remembers the uncropped source on first crop; el.cropRotation + el.cropRect (normalized 0–1) record the parameters for re-editing. Apply uses a hidden canvas to translate→rotate→drawImage at source resolution, then extracts the user's rectangle and writes the result as a fresh PNG asset (img_crop_<uid>). Element height is auto-adjusted to keep the new aspect (width preserved). After a crop, el.isCompressed is cleared since the output is a fresh PNG; the user can re-compress to WebP if needed.
- Fix: collapsed chevron now correctly points right (▶). v0.16.34 left the CSS `transform: rotate(-90deg)` on `.panel-section.collapsed .collapse-icon` as a "fallback" alongside the new polyline-points swap. Turns out the SVG CSS transform DOES render in this browser after all — they were compounding, so my right-pointing polyline got rotated -90° → ▲ up. Removed the redundant CSS rule; the polyline swap in initCollapsiblePanels is now the only mechanism.

v0.16.35 - May 2026 — Engine v2.12
- Auto-Resize engine v2.12: `placeMainImage`'s slot-collapsed recovery. On thin banners (728×90, 320×50) the heading + CTA pair often eats the entire safezone width, leaving `slot.w` ≤ 0. Pre-v2.12 this fell straight through to a 30–80px center placement, which left the main image as a marooned thumbnail regardless of the v2.10 size floor or the v2.8 cover-fallback. Now: when the computed slot is degenerate, fall back to the FULL safezone before doing anything else, so the cover + size-floor logic still has room to work. main-image renders at the bottom of the z-stack, so heading and CTA layer over it cleanly. Net effect: large hero-style image on 728×90 / 320×50 instead of a tiny marooned thumbnail.
- Fix: thin red ring no longer leaks into full-preview mode. previewFrameNode already sets `canvas.style.boxShadow = 'none'` inline, but anything that could re-apply the active-canvas accent ring was getting through anyway. Added a belt-and-braces CSS rule: `body.preview-active .canvas` (and `.canvas-frame.active .canvas` under preview-active) now have `box-shadow / outline / border` forced to `none / 0` via `!important`. In RMIT theme this was a 1.5px red ring; under default purple theme it was less obvious but still wrong. Reads like the deployed ad now.

v0.16.34 - May 2026 — Engine v2.11
- Fix: collapsed panel chevron now points right (▶) as intended. v0.16.32 used `rotate(90deg)` for the collapsed state which actually rotates the default down-pointing chevron CLOCKWISE — apex moves to the LEFT side, giving ◀. Corrected to `rotate(-90deg)` so the apex moves to the right.
- Chevron also nudged a touch closer to the panel's left edge via `margin-left: -4px` on `.collapse-icon`, so the label still starts where it used to and the chevron sits more snugly in the gutter.
- Auto-Resize engine bumped to v2.11: source layer order, groups, and masks are now preserved through a resize. Placement rules still run in role-priority order (heading before cricos, etc. — they read each other's geometry off ctx.placedElements), but target.elements is rebuilt in source array order at the end. Result: groupId siblings stay adjacent the same way they were on the source, and mask-above-image positional pairs survive intact.
- groupIds are remapped per-target — a fresh gid for each distinct source group on each target canvas — because groups can't span canvases (multi-select would otherwise drag elements across canvases).
- Mask post-pass switched to positional detection (matches `findMaskAbove`'s convention from script.js). The old code relied on `maskTargetId`, a field legacy v0.16.26 masks don't carry — positional detection works regardless of which version of the mask system created the mask.
- Singleton groups — `groupId` that ends up with only one remaining member after drops (e.g., a mask whose image was dropped by a disabled role rule, or a group whose other members got pruned) — are auto-cleared. Cleans up the "break link groups for those that no longer support it" half of the user request.

v0.16.33 - May 2026 — Engine v2.10
- Export dialogue gains a Data version dropdown (when the project has data rows). Pick a specific row to bake into the export, or "All versions (separate folders)" to route to the existing per-row folder export. The separate "Export All Versions" button is gone — the dropdown subsumes it. PNG path now also honours the chosen version (it didn't before — PNGs silently rendered whatever was active in the editor).
- Auto-Resize engine bumped to v2.10: main-image size floor for thin-banner canvases. After the cover-fallback fires for canvasAspect > 3, enforce that the image's larger dimension is ≥ 40% of the canvas's larger dimension. Stops the image from coming out marooned at ~80px on a 728×90 when the slot between heading and CTA is narrow. canvas overflow:hidden + clampToCanvas exemption already handles the overflow crop cleanly.
- Browser tab title now reads `<project name> - RMIT Adflow`. Driven from `render()` so renames, project loads, new-project creation, and even undo/redo all keep it in sync without per-call-site hooks.
- Middle-click guard extended. Was only blocking `<button>` / `[role="button"]` — middle-clicking element transform/rotation/radius/thickness handles still fired them as if it were a left-click. The capture-phase guard now also matches `.handle` and `.panel-fullscreen-btn`. Canvas-area and element-wrapper middle-clicks are still allowed through so the pan-by-middle-drag affordance keeps working.
- Fix: single-preview mode no longer shows a thin accent-coloured ring around the canvas. `.canvas-frame.active .canvas` adds a 1.5px box-shadow using `--accent-base` (red on RMIT theme, purple on dark) for the active canvas in the editor — which leaked into the single-preview render too, very visible on solid blue backgrounds. Inline `box-shadow: none` is now applied when `isSinglePreview` so the preview reads like the deployed ad.

v0.16.32 - May 2026 — Engine v2.9
- Panel section collapse chevrons moved to the left of the panel name (Figma/Photoshop convention) and re-styled. Accent-purple colour instead of muted grey, slightly thicker stroke (2 → 2.5), and the collapsed-state rotation flipped from -90deg (left-pointing ◀) to +90deg (right-pointing ▶) to match the new left-side placement.
- Implementation: CSS-only reorder via flex `order: -1` on `.collapse-icon`, with `.panel-header-collapsible` switched from `justify-content: space-between` to `flex-start` + a `gap: 8px`. Any non-chevron, non-label child (e.g. the dynamically-injected per-section fullscreen button, or the Assets panel's New-folder/+ action buttons) gets `margin-left: auto` so it sticks to the far right of the header. One HTML tweak in `index.html` for the Assets panel: lifted the `<svg class="collapse-icon">` out of the nested actions `<span>` so it's a direct child of the `<h3>` and the same CSS rules apply uniformly to every header.
- Hover colour bumped accent-purple → accent-light to keep the same visual feedback step.

v0.16.31 - May 2026 — Engine v2.9
- Fix: double-clicking a masked group now consistently selects the mask SHAPE (not the image underneath). Previously, when entering isolation mode via dbl-click on the visible masked area, the dbl-click usually landed on the image's wrapper (the mask shape's own children are visibility:hidden, so hits often pass through to the image below). The outline would correctly show the mask, but the properties panel showed image props. Selection-deselect-reselect was the workaround.
- Fix scoped to the element dbl-click → isolation path: when the target element is an image AND there's a mask shape directly above it (`findMaskAbove`), the selection is re-routed to the mask. Non-mask groups are unaffected. Dbl-clicking the mask wrapper directly still works as before (no re-route needed since `el` is already the mask).

v0.16.30 - May 2026 — Engine v2.9
- Export is now its own top-level menu section, separated from File. The menu item is renamed "Export…" (with the dialog-affordance ellipsis) — clicking it opens the revamped Export dialogue. The canvas right-click "Export → HTML5 / PNG" submenu remains unchanged: it still exports the active canvas directly without going through the dialogue.
- Export dialogue revamped with four working pieces:
  • Filename prefix input — defaults to the project name (sanitised), but you can rename it just for the export without touching `state.projectName`. The output files are still named `<prefix>_WxH.zip` / `.png`.
  • Format selector — HTML5 ZIP (default) or PNG. PNG exports the active frame as a static image, one file per selected canvas.
  • "Skip frames marked as skipped" toggle — default on. Off includes flagged frames in the HTML5 export. The toggle disables itself with a help hint when no frames are currently flagged. PNG always exports the active frame regardless.
  • Per-canvas checkbox list — already existed, now reuses canvas names alongside size + estimated KB + click tag.
- `exportCanvasAsZip(c, options)` and `exportCanvasAsPng(c, options)` now accept an options object: `{filenamePrefix, includeSkippedFrames}`. The right-click canvas exporters pass nothing — same direct-export behaviour as before. The Export dialogue plumbs the user's choices in. `generateExportHTML` reads a transient `state._exportIncludeSkippedFrames` flag set by the callers, so the override is local to each export rather than a persistent setting.
- "Export All Versions" button now sits alongside "Export Selected" and is clearly labelled as ZIP-only (the data-merge per-version flow doesn't have a PNG counterpart).

v0.16.29 - May 2026 — Engine v2.9
- Mask connector line moved onto the icon column. Was sitting in the far-left gutter (left:1px), now sits directly under the layer icons at left:12px (the icon centre line, given a 13px-wide icon at 6px left padding). The line is also shorter — clipped to the row padding zones above/below the icon — and 1px thicker (2px wide instead of 1px). Net effect: a small accent-purple bridge that visually connects the two icons rather than a vertical mark on the side of the panel. Still uses the transparent-fade gradient at the icon-side end so it doesn't butt up against the glyph.

v0.16.28 - May 2026 — Engine v2.9
- Mask connector line in the Layers panel is much less intrusive now. The thin accent-purple line is moved out of the icon column entirely (sits at the very left edge of each row in the 2-pixel gutter) and uses a vertical gradient that fades to transparent at the far end of each row — so it never reads as a hard border running across the icons. The end-caps (the dots at 50% of each row) are gone. The connector is strongest in the gap between the two rows where the mask and its image meet, and dissolves softly into the panel.
- CSS fix: `.dropdown-item.has-sub:hover .sub-dropdown` was using a descendant combinator, which would have cascade-opened every nested sub-dropdown at once if we ever nested two levels deep. Now uses the direct-child combinator `>` so only the immediate sub-dropdown opens on hover. No behaviour change for the existing single-level menus.
- Reorganised the hamburger main menu into clearer sections with submenus where they help:
  • FILE: New Project… | Open ▶ (From File / From Cloud) | Open Recent ▶ | Save ▶ (Save Project / Push to Cloud) | Export HTML.
  • PROJECT (new section): Project Settings… | Data & Versions… (these used to be loose in File).
  • EDIT: Undo | Redo (unchanged).
  • VIEW: Clear All Guides (unchanged).
  • HELP ▶ collapsed into a submenu: Shortcuts | Documentation.
  • App-level at the bottom: Settings… | About (unchanged).
- All existing menu-item IDs are preserved, so every previously-wired click handler (and auth-ui.js's "hide cloud items when signed out" toggle for `menu-file-cloud` / `menu-file-push`) continues to work — those items now just live inside the Open ▶ / Save ▶ submenus.

v0.16.27 - May 2026 — Engine v2.9
- Open Recent menu now shows both local and cloud saves in two clearly-labelled sections. "Local" lists the IndexedDB-cached recent project snapshots (unchanged — up to 10 entries with the existing zero-friction one-click reload). "Cloud" lists the user's 10 most-recently-updated Supabase projects (queried via `sb.from('projects')` filtered by current user, ordered by `updated_at DESC`). Click a cloud entry to confirm and pull via the existing `pullCloudProject(row)` codepath.
- Cloud section only renders when the user is signed in. No sign-in nag when signed out — the local list stands on its own.
- The submenu refreshes on hover (`mouseenter` on `menu-file-recent`) so signing in mid-session immediately surfaces the Cloud section without needing a save. Still also refreshed after each save so newly-pushed cloud projects show up at the top of the list.
- Timestamps in the Cloud section use `_formatRelativeTime()` when available (matches the Cloud Projects modal styling), falling back to a short locale string. Local section timestamps unchanged.

v0.16.26 - May 2026 — Engine v2.9
- Mask + image are auto-grouped. When you set a shape as a mask via the right-click "Use as mask" menu, the shape and the image directly below it now share a `groupId` automatically — so the pair moves and scales as a unit by default. If either side already belongs to a group, that group is reused (we don't tear apart pre-existing groups). Removing the mask intentionally does NOT auto-ungroup; the user can ungroup via Ctrl+Shift+G when they want.
- Mask connector line in the Layers panel. A thin accent-purple line + small dot bridges the mask shape's layer row and its image's layer row, so the relationship between the two is visible at a glance. Drawn via CSS pseudo-elements in the left gutter — no extra DOM. Appears whenever the mask is "active" (`isMask` set, not hidden, and the layer directly below in z-order is an image).
- New keyboard shortcuts, Illustrator-style: Ctrl+2 locks the current selection; Ctrl+Shift+2 unlocks it. Strict (not toggle) so the muscle memory works regardless of mixed-lock state in a multi-select. No-op when nothing's selected. Standard pushHistory + toast feedback ("Locked N layers." / "Unlocked N layers.").

v0.16.25 - May 2026 — Engine v2.9
- Fix: gradient fills now actually render on RMIT Pixel shapes (and any other SVG-rendered element). Previously, assigning a gradient — via the colour picker, the new Saved Gradients swatches in v0.16.24, or by typing a `linear-gradient(...)` value directly — left the pixel rendering black, because SVG's `fill="..."` attribute silently ignores CSS gradient strings. Rect, circle, line, button and text were unaffected; they use CSS `background:` which natively supports `linear-gradient(...)`. Pre-existing bug, surfaced because the new Saved Gradients feature made it trivial to apply the same gradient repeatedly.
- New helper `svgFillForCssColor(value, idSeed)` in script.js: parses the CSS gradient via cpParseGradient, computes the matching SVG endpoint coords from the CSS angle (CSS 0° = upwards, 90° = rightwards), emits an inline `<defs><linearGradient>` block, and returns `{defs, fillAttr: "url(#svgrad_<elId>)"}`. Each element gets a unique `id` so multiple pixels with different gradients don't collide.
- SVG doesn't natively support CSS color hints (midpoint balance) — the helper approximates them by inserting a synthetic stop at the hint position whose colour is the 50/50 mix of its two neighbours. Visually close enough for the common bias range.
- Same fix applied to the export pipeline (`export-pipeline.js`), so exported HTML5 banners render pixel gradients correctly too.

v0.16.24 - May 2026 — Engine v2.9
- Gradient system revamp. Three things:
- (1) Midpoint balance markers. Each pair of adjacent colour stops now has a small diamond-shaped midpoint marker between them on the gradient track. Drag the diamond to bias where the 50/50 transition sits (the marker stays interior to its two surrounding stops, clamped to 5–95% of the gap). Double-click resets to 0.5 (linear). Under the hood: each stop carries a new `mid` field (0..1) representing the transition midpoint to the next stop in pos-sorted order; cpBuildGradient emits a CSS color hint (`linear-gradient(angle, color 0%, 30%, color 100%)`) between any two stops where mid ≠ 0.5, and cpParseGradient round-trips the same. Mid defaults to 0.5 (linear) so existing gradients are unchanged.
- (2) Saved gradients. New "Saved Gradients" row in the color picker, above the existing solid palette. Click + to save the current gradient (only enabled while the gradient tab is active). Click a saved swatch to load it back into the editor. Right-click a saved swatch to remove it. Stored as structured `{angle, stops}` entries in `state.savedGradients` rather than CSS strings, so future schema changes won't break old saves.
- (3) Hide-for-incompatible-keys. The Saved Gradients row hides entirely when the picker is open on a property that doesn't accept gradients (currently just `strokeColor` — the gradient tab was already hidden there, the swatch row now follows suit).
- Both `state.savedPalette` (existing) and `state.savedGradients` (new) are deep-cloned into the project file by buildFlowBlob — palette + gradients persist with the working file across saves, loads, and cloud pushes.

v0.16.23 - May 2026 — Engine v2.9
- The RMIT theme is a light-background theme (`color-scheme: light`, `--bg-body: #f4f4f4`), so it now also gets the dedicated `Adflow_lighttheme.svg` wordmark — same as the Light theme. Previously only `state.theme === 'light'` swapped to the light-theme logo, so the dark wordmark looked muddy against RMIT's light panels.
- Refactor: `syncAdflowLogos()` now consults a small `LIGHT_BG_THEMES` set (currently `{'light', 'rmit'}`) instead of comparing to a hardcoded string. Adding future light themes is a one-line edit to that set.

v0.16.22 - May 2026 — Engine v2.9
- Fix regression from v0.16.20: middle-click panning works again. The earlier middle-click guard was too aggressive — it swallowed every middle-mouse mousedown in capture phase, which also killed the workspace pan-by-middle-drag affordance (the canvasArea + onElementMouseDown handlers both start a pan on `e.button === 1`). The guard is now scoped to `<button>` and `[role="button"]` targets only, so it still blocks middle-click from firing the per-canvas frame controls and single-preview toggle, but the canvas and element layers see middle-click through as before. Pan/scroll behaviour matches pre-v0.16.20.

v0.16.21 - May 2026 — Engine v2.9
- Auto-Resize engine bumped to v2.9: CRICOS font sizer gains a third candidate (`height × 0.012`) alongside the existing minDim and width formulas. The fontSize is now `max(minDim × 0.023, width × 0.008, height × 0.012)`, all clamped to [4, 7]. Specifically fixes 160×600 (Wide Skyscraper) where minDim and width were both 160 and both clamped to the floor of 4 — the new height-driven candidate gives 7 there instead. CRICOS goes from 4 → 7 on 160×600. No effect on other listed ad formats — 300×250, 300×600, 728×90, 970×250, 320×50 already reach their existing size via the minDim or width formula.

v0.16.20 - May 2026 — Engine v2.8
- Middle-click no longer triggers buttons. Several mousedown-based handlers in the app (per-canvas frame controls — prev/next/add/remove frame, the single-preview toggle) weren't filtering `e.button`, so middle-clicking them fired the same action as left-click. Added a global capture-phase mousedown listener that swallows button=1 events: `preventDefault` + `stopPropagation`. Also kills the browser's middle-click autoscroll cursor inside the app. Nothing in the codebase relied on middle-click — `onCanvasHeaderDrag` and the canvas surface mousedown handler already early-returned on it, so this just makes the rest of the buttons match.
- Auto-Resize engine bumped to v2.8: main-image cover-fallback threshold is now canvas-aspect-aware. On thin banners (canvas aspect > 3 either direction — 728×90, 320×50, 970×250, 160×600) the contain→cover trigger lifts from 0.6 fill to 0.9 fill, so cover almost always fires there. Result: main image fills the slot's smaller dimension fully, with the larger dimension overflowing into the canvas margins (clampToCanvas already exempts main-image from overflow clamping, so canvas overflow:hidden handles the crop during preview/export). Stops thin formats from showing a tiny marooned image in the middle of the layout. Normal-aspect canvases (300×250, 300×600, 1:1 squares, etc.) keep the 0.6 threshold and are unchanged.

v0.16.19 - May 2026 — Engine v2.7
- Swapped the two Auto-Resize entry points so the slow path and the fast path live in opposite places. The workspace Auto-Resize button (bottom-left of the screen, anchored to the left panel) now ALWAYS opens the picker dialogue — source canvas + multi-select targets — regardless of any setting. The canvas right-click "Auto-Resize" entry now ALWAYS resizes instantly from the active canvas (the one whose menu was just opened) into every other canvas, no popup. Previously the button honoured the `showCanvasSelection` flag (default on → popup) and the context menu was hardcoded to popup.
- Removed the now-dead `showCanvasSelection` setting. The "Show canvas selection dialogue" checkbox is gone from the Auto-Resize Settings modal, replaced by a one-line caption that explains the new split: "The workspace Auto-Resize button always opens the picker dialogue. The canvas right-click Auto-Resize entry always resizes instantly from the active canvas into every other canvas." `getAutoResizeSettings()` strips `showCanvasSelection` from older autosave blobs on load — same migration pattern used for `showProgress` in v0.16.16.
- The "Include unassigned by default" toggle is unchanged — it still applies to both entry points and now reads as the default for the dialogue's misc-elements checkbox + the value the context menu uses for its instant run.

v0.16.18 - May 2026 — Engine v2.7
- Right-clicking the canvas header (the "W × H" dimensions label floating above each canvas in the workspace) now opens the same context menu as right-clicking the canvas surface with no element selected — Preview, Auto-Resize, Add Element submenu, Change BG color, Export, Clear all, etc. Previously this fell through to the workspace background menu (just Snapping / Rulers / Settings).
- Fix in the global contextmenu handler in script.js: after `closest('.canvas')` returns null, fall back to `closest('.canvas-header')` and resolve to the sibling `.canvas` via the parent `.canvas-frame`. No effect on element right-clicks or left-panel canvas list right-clicks.

v0.16.17 - May 2026 — Engine v2.7
- New "Clear all" option: "Other canvases" wipes every canvas EXCEPT the active one. Sits between "Current canvas" and "All canvases" in the right-click context menu submenu, and as a middle red-bordered button in the canvas Properties panel's Clear-all row.
- The active canvas (its elements, selection, and any link-group memberships on it) stays untouched. Link groups whose only remaining members were on wiped canvases are automatically pruned via cleanupLinkGroups. If there's only one canvas in the project, the action shows a "No other canvases to clear" toast instead of prompting.
- Properties-panel button labels shortened from "Current canvas" / "All canvases" to "Current" / "Others" / "All" so three buttons fit comfortably in the narrow right panel; tooltips on hover still spell out the full scope.

v0.16.16 - May 2026 — Engine v2.7
- Removed the fake "AI" progress overlay from Auto-Resize entirely. The terminal-styled pipeline panel with the spinner, scripted pid + UTC header, and 10 random-weighted "[✓] Initializing visual analysis pipeline" status lines is gone — it was pure theatre that gated the render() for 2–3 seconds while the placement work had already completed. Results now render instantly when Auto-Resize finishes.
- The `showFakeAutoResizeProgress` function (about 130 lines) and the `showProgress` setting (both the `behaviour.showProgress` flag and the mirrored top-level field) are removed from `auto-resize-engine.js`. The "Show technical progress overlay" checkbox in the Auto-Resize Settings modal is gone. The "Show canvas selection dialogue" hint text no longer references the now-deleted progress overlay.
- Existing projects with `showProgress` keys in their autosave blobs get those keys quietly stripped on load via `getAutoResizeSettings()` — no migration banner, no data loss elsewhere.

v0.16.15 - May 2026 — Engine v2.7
- Faster cold loads on the deployed Netlify build. Removed `?_t=Date.now()` cache-busters from the RMIT asset fetches (`data/assets/manifest.json`, the directory-listing fallback, and every per-file fetch in `syncRmitAssets`). Those query strings were forcing the browser AND the Netlify edge to bypass cache on every page load, so each visit re-downloaded the manifest + every RMIT image fresh. Locally that's a microsecond filesystem read; on Netlify it's a full HTTP round-trip per file.
- Parallelised the RMIT asset preload loop. The `for (const filename of filenames)` with sequential `await fetch(url)` calls inside `syncRmitAssets` was costing N× RTT on cold loads. Switched to `Promise.all(filenames.map(...))` so all assets fetch concurrently; final library order is still preserved by iterating the resolved results.
- No behaviour change otherwise — same fallback chain (manifest → directory listing → hardcoded defaults), same error handling per asset.

v0.16.14 - May 2026 — Engine v2.7
- Internal refactor: extracted the custom color picker (iro.js wrapper) into a new file `color-picker.js`. Moved the top-level picker state (iroPicker, currentCpKey, cpIsGradient, cpGradStops, cpActiveStop), gradient helpers (cpStopCss, cpBuildGradient, cpParseGradient, cpSyncGradientUI, cpRebuildStops, cpColorAtPos, cpAddStop, cpRemoveStop), and the public API — initColorPicker, renderPalettes, updateCurrentColor, emitColorUpdate, openColorPicker, closeColorPicker, syncColorPickerWithSelection — about 484 lines.
- script.js dropped from 11,966 → 11,482 lines.
- This completes the Option A refactor. Over five minor versions (v0.16.9 → v0.16.14), the script.js monolith has gone from 16,082 lines down to 11,482 lines — a 29% reduction. Pulled out into focused files: auto-resize-engine.js, docs-content.js, auth-ui.js, data-merge.js, export-pipeline.js, color-picker.js (plus the existing font_assets.js). Each file is independently node-checkable, has a header comment documenting its dependencies and load order, and depends on script.js globals only at call-time.
- Loading order in index.html (top → bottom): auto-resize-engine.js → docs-content.js → auth-ui.js → data-merge.js → export-pipeline.js → color-picker.js → script.js. Classic <script> tags share the global lexical scope, so consts/lets in earlier files are visible to script.js function bodies.
- No user-facing change. Color picker, gradient editor, swatches, recent-colors palette, and selection sync all behave identically.

v0.16.13 - May 2026 — Engine v2.7
- Internal refactor: extracted the HTML5 export pipeline out of script.js into a new file `export-pipeline.js`. Moved: getRequiredFonts (font subset detection), exportCanvasAsZip (primary HTML5 ZIP), exportCanvasAsPng (static image), clearCanvasFrame (frame-scope cleanup helper), generateExportHTML + _generateExportHTMLRaw (the index.html template builder — the largest single function in the app), and openExportModal + its two click handlers.
- About 862 lines lifted out. script.js dropped from 12,828 → 11,966 lines.
- The project save/load functions (buildFlowBlob, saveProjectAsFlow, loadProjectFromBlob) intentionally stay in script.js — those are project persistence, not export. The cloud-push hook in auth-ui.js still calls buildFlowBlob via the shared script realm.
- No user-facing change. ZIP export, PNG export, image-set export, all-version export, and the Export modal table all behave identically.

v0.16.12 - May 2026 — Engine v2.7
- Internal refactor: extracted the Live Data / Versioning system out of script.js into a new file `data-merge.js`. Moved 38 `dm*` helpers (dmEsc, dmFieldsForType, dmSlotKey/Name, dmResolveImage, dmGroupSyncsField, dmFieldActive, dmOverridesForRow, dmDisplay, dmIsDynamicEditable, dmWriteCell, dmToggleField, dmDiscoverSlots, dmParseCSV/CsvCell/ToCSV/ImportCSV/ImportFile/ExportCSV, dmSetActiveVersion, dmToggleLock, dmBakeRow, dmRunExport, dmActiveRowForOutput, dmExportAllVersions, dmAddColumn/RenameColumn/ReorderColumns/ReorderRows/SortByColumn, dmAddRow/DeleteRow), the data panel UI (openDataPanel, dmRenderPanel, dmWirePanel, _dmState), version switcher rendering (renderVersionSwitcher, renderPreviewVersionBar, cycleVersion), and the DM_FIELD_LABEL constant.
- About 803 lines lifted out. script.js dropped from 13,631 → 12,828 lines.
- One micro-edit during extraction: the data-merge click handler on the properties panel previously read the `propsEl` variable (defined in script.js). Since data-merge.js loads before script.js, that variable wouldn't exist at handler-attach time — swapped for `document.getElementById('props')`, which is safe either way.
- No user-facing change. Live Data panel, dynamic slot binding, CSV import/export, version switcher, and all-version export behave identically.

v0.16.11 - May 2026 — Engine v2.7
- Internal refactor: extracted the entire Supabase auth + Cloud Projects + Team Spaces stack out of script.js into a new file `auth-ui.js`. Moved: the Supabase client init (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `_authStorage`, `sb` client), the `authState` IIFE (session bootstrap + sign-in/up/out wrappers), the `spacesState` IIFE (space list cache + CRUD), the top-bar auth chip rendering, the sign-in/sign-up modal, the Cloud Projects modal (push/pull/list/delete), space management + members + invitations, and the splash auth gate.
- That's ~924 lines lifted out of the monolith. script.js dropped from 14,555 → 13,631 lines.
- `auth-ui.js` loads BEFORE script.js in index.html (after `auto-resize-engine.js` and `docs-content.js`). This matters because script.js's boot IIFE references `authState.enabled`, `authState.ready`, and `authState.currentUser()` at load-time — those globals must exist by then. Function-body references from script.js to auth/cloud globals (in `saveProjectAsFlow`, etc.) fire only at click-time, after both files have loaded.
- Anonymous local use remains completely unchanged. When `SUPABASE_URL` / `SUPABASE_ANON_KEY` are blank or the Supabase SDK fails to load, `sb` is null, `authState.enabled` is false, the chip hides, and no network calls fire — exactly as before.

v0.16.10 - May 2026 — Engine v2.7
- Internal refactor: extracted the in-app documentation system and changelog data out of script.js into a new file `docs-content.js`. Moved: `DOCS_SECTIONS` (the full Help → Documentation content tree — getting-started, multi-canvas, animation, link groups, auto-resize, dynamic data, export, shortcuts, changelog), `openDocumentation` + `renderDocsPanel` (modal scaffolding and sidebar navigation), `CHANGELOG_DATA` (every release entry from v0.1.0 forward), `generateChangelogHtml` (shared renderer used by both the changelog viewer and the post-update splash), and `openChangelogModal` itself.
- That's ~1,360 lines lifted out of the monolith. script.js dropped from 15,919 → 14,555 lines.
- `checkVersionUpdate` (the post-update splash) intentionally stays in script.js — it's tightly bound to the boot flow and the hardcoded `currentVersion` constant. It still calls into `generateChangelogHtml` and `openChangelogModal` via the shared script realm, since classic `<script>` tags load into the same global lexical scope.
- `docs-content.js` loads before script.js in index.html (same pattern as auto-resize-engine.js). Top-level `const` bindings in docs-content.js are visible to all later scripts; function bodies in docs-content.js that call into script.js (`openModal`, `syncAdflowLogos`) only do so at click-time, by which point script.js has fully loaded.
- No user-facing change. Documentation modal, changelog modal, About dialog, and the post-update splash all behave identically.

v0.16.9 - May 2026 — Engine v2.7
- Internal cleanup: deleted the legacy `autoResizeFromSelected` executor and its four helpers (`canvasFormatClass`, `detectElementRole`, `syncDefaultsForRole`, `layoutForRole`) from script.js. That code dated back to before the v2 rule engine and hadn't been called from anywhere since the FAB / context-menu entries switched to `runRuleBasedAutoResize`. Removing it trims roughly 170 lines from the monolith and eliminates the last reference to the old per-format-preset model, so future agents won't have to wonder which executor is canonical.
- No user-facing change. Auto-resize behaviour, settings, and engine version (v2.7) are unaffected — the new engine in `auto-resize-engine.js` already handled every entry point.
- First step in a planned multi-file refactor (Option A) — script.js will lose `docs-content.js`, `auth-ui.js`, `data-merge.js`, `export-pipeline.js`, and `color-picker.js` over the next few minor versions to bring the monolith back under control.

v0.16.8 - May 2026 — Engine v2.7
- Undo/redo system overhauled. Default depth jumps from 10 to 50 steps, and the long-standing bug where the engine capped at a hardcoded 15 entries regardless of the configured limit is fixed — the configured limit now actually applies.
- Snapshot fields expanded. The undo stack now also captures `frames` (timeline durations / transitions / skip flags), `activeFrameId` (which frame you were on), and `projectName` (renames). Previously those weren't undoable. Settings-toggle-style state — theme, auto-resize behaviour toggles, view preferences (rulers / safezones / snap), zoom, scroll position, history-limit itself — intentionally remains EXCLUDED, since "undoing a preference" isn't expected behaviour.
- Re-entrancy guard added. A new `_restoringHistory` flag short-circuits any `pushHistory()` call that fires DURING a restore — prevents the restore itself from polluting history with a duplicate of the snapshot we just popped (could happen via downstream render→handler chains).
- Settings UI: history-limit max raised from 50 to 100. Default value in the field is now 50. Minimum 5 (the old minimum was 1, which made undo functionally useless).
- One-time migration on autosave restore: projects with the old default (savedHistoryLimit ≤ 10) get bumped to 50 automatically. Customised values above 10 are preserved.
- Cleanup: removed the clever-but-fragile non-increment trick in the trim path; historyIndex is now set explicitly after each push, and decremented explicitly on each oldest-shift. Easier to reason about, identical behaviour.

v0.16.7 - May 2026 — Engine v2.7
- Light theme now uses a dedicated Adflow wordmark — `data/Elements/Adflow_lighttheme.svg` — so the brand reads cleanly against the lighter panel background. Every other theme (default dark, ocean, navy, RMIT) continues to use `Adflow_logo.svg`.
- The swap is JS-driven via a new `syncAdflowLogos()` helper that walks every `<img data-adflow-logo>` in the DOM and sets the right src based on `state.theme === 'light'`. Called from `render()` immediately after the theme class is applied, so theme changes update every wordmark in place without a reload.
- Four locations marked with the `data-adflow-logo` attribute and swapped on theme change: boot splash logo, topbar logo, size-overlay logo (shown on tiny viewports), Documentation "Welcome to Adflow" hero image. The docs welcome image is generated inside a template literal at modal-open time, so the docs renderer now calls `syncAdflowLogos()` after the panel HTML is inserted to catch that one.

v0.16.6 - May 2026 — Engine v2.7
- Auto-resize button stripped down to just its label. Removed the sparkle SVG icon on the left and the "AI" pill badge on the right — button now renders as a plain "Auto-resize" label on the purple gradient. The matching CSS rules for `.ar-btn-icon` and `.ar-btn-ai-badge` were dropped from styles.css since they're no longer referenced anywhere.

v0.16.5 - May 2026 — Engine v2.7
- Auto-Resize documentation completely rewritten and significantly expanded. The previous 4-subsection summary (which referenced the removed Tools panel and the legacy format-preset executor) is replaced with a deep 7-subsection technical reference covering the v2.7 rule-based engine end-to-end.
- New subsections: (1) Overview & philosophy — design principles, two entry points, engine versioning rationale. (2) The 9-role taxonomy — full role table with priorities, strategies, required flag, and a paragraph on why priority drives placement order. (3) Role detection & manual override — the 5-step heuristic ladder, idempotent refresh behaviour, the layer-panel role icon for manual locks. (4) Placement rules — every rule documented individually with its anchor, size formulas, font formulas, and mode case-analysis (background-image / rmit-logo / cta-button / heading / subheading / cricos / main-image / rfwn / extra-info / misc). (5) Cross-role relations & post-placement passes — R1 / mask post-pass / no-touch collisions / canvas-bounds clamp, with notes on pass ordering. (6) Settings, live linking & engine versioning — every settings option explained, live-linking property semantics, full engine version history v2.0 → v2.7. (7) Workflow & tips — best practices, reference canvas data table, internal architecture pointer.
- Also updated the Getting Started "Auto-Resize at a glance" snippet and "Your first project" walkthrough to reference the new panel-anchored Auto-resize button and the right-click "Auto-Resize" entry rather than the obsolete Tools-panel button.

v0.16.4 - May 2026 — Engine v2.7
- Canvas context menu: Auto-Resize moved up directly under Preview at the top of the menu, styled as a sibling "highlight" entry — same purple accent color, same icon-on-left layout, same gap spacing as Preview. Sparkle SVG (filled, current colour) replaces the previous ✨ emoji + label. Text shortened from "Auto-resize from this canvas" to "Auto-Resize" to match Preview's terse one-word treatment.
- Clicking the menu entry now always opens the canvas-selection dialogue, regardless of the engine's "Show canvas selection dialogue" setting. The FAB's instant-resize bypass doesn't apply here — reaching for the context menu implies you want to choose targets each time. The FAB still respects the setting for one-click iteration loops.

v0.16.3 - May 2026 — Engine v2.7
- Auto-resize anchor restyle: background switched from `--bg-panel` (#121419) to `--bg-body` (#0b0c0f) so the strip reads as a slightly darker base than the panel above it — clearer separation from the scrolled section list.
- Slightly less tall: container padding 10 px → 8/10 px (vertical/horizontal), main button padding 10/14 → 7/14 px, border-radius 8 → 7 px, settings button 38×38 → 32×32 px, settings icon 15 → 14 px. Trims roughly 6 px off the total anchor height while keeping the same proportional balance.

v0.16.2 - May 2026 — Engine v2.7
- Auto-resize button toned down and re-homed. No longer a flashy floating FAB pinned to the viewport — it now lives as a prominent button anchored at the bottom of the left panel column, outside the scroll area so it stays visible regardless of scroll position.
- Visual restraint: dropped the resting pulse animation entirely. The button just sits there. Hover lifts it 1 px with a stronger purple glow (16 px outer + 22 px inner halo) and a brightness boost. Click feedback: scale to 0.97 with a tighter shadow. No animation unless interacted with.
- Form factor: same purple gradient (#7c5cff → #a78bff) but lower-key — 10/14 px padding instead of 13/22, 8 px border-radius instead of 14, 12.5 px label instead of 13.5, 0 2px 8px shadow instead of 0 6px 22px. Reads as a "more prominent version of a regular button" rather than a floating action button.
- "AI" pill badge preserved next to the label — tiny white-translucent capsule, kept from the original Tools-panel button.
- Settings (gear) button beside it, sized 38×38 px, matched dark-input background with subtle hover border highlight.

v0.16.1 - May 2026 — Engine v2.7
- Fix: Auto-resize FAB wasn't surfacing on top of the workspace. The boot splash overlay has z-index 999999 and stays in the DOM (opacity 0 + pointer-events none) after the app loads — its stacking context was burying the FAB's z-index 9000. Bumped FAB z-index to 999998 (just below splash) with !important on the position properties so nothing in the workspace can claim a higher stacking order. Hard refresh required if the CSS file was previously cached.
- New "✨ Auto-resize from this canvas" entry in the canvas right-click context menu. Sits above the Clear all submenu, accent-coloured + bold to mark it as the primary creative action. Triggers the same dispatcher the FAB uses, so it honours both the "Show canvas selection dialogue" and "Include unassigned" settings — clicking either surface produces identical behaviour. Resizes from whichever canvas is currently active (right-clicking a canvas already makes it active before the menu opens).

v0.16.0 - May 2026 — Engine v2.7
- Tools panel removed from the right side. The Auto-resize button is now a prominent floating action button pinned to the bottom-left of the viewport, sized and styled to feel like the editor's primary action. Gradient purple pill (linear-gradient #7c5cff → #c4b3ff), sparkle icon, "Auto-resize" label, 14 px corner radius, layered shadows + inset highlights.
- The FAB has a 3.2-second resting pulse animation (subtle box-shadow breathing between rgba(124,92,255,0.42) and a brighter rgba(124,92,255,0.78) outer + 24 px rgba(167,139,255,0.5) inner halo) that draws attention without being distracting. On hover, the pulse pauses and the button lifts by 3 px with a stronger 40 px halo and ~10% brightness boost. On click, it scales down to 0.97 with a sharp 50 px halo flash for tactile feedback.
- A smaller, quieter settings (gear) button sits beside the main FAB with backdrop-blur, purple-tinted border, and matching hover/active lift. Opens the Auto-Resize Settings modal as before.
- "Clear everything" removed from the Tools panel. Replaced by a "Clear all" entry in:
  • The canvas context menu — submenu offering "Current canvas" / "All canvases", colour-coded red for danger.
  • The canvas Properties panel — two red bordered buttons side-by-side in a "Clear all" row, below the Download buttons.
- "Current canvas" wipes every element on the active canvas (preserving link groups elsewhere); "All canvases" wipes every canvas in the project and resets state.linkGroups. Both prompt for confirmation and push history so Ctrl+Z restores. Tooltips on both buttons explain the scope.

v0.15.11 - May 2026 — Engine v2.7
- RFWN width loosened back to fit "what's next" on a single line. v0.15.9's tightening (font × 6.2, cap 80) was too aggressive — on 970×250 at font 16, "what's next" overflowed and the tagline wrapped to 3 lines. New: font × 6.8 with cap 100, so the longer of the two natural lines fits as a single row while keeping the box snug.
- Wide-banner heading (h ≤ 100, aspect > 2) now vertically centres in the canvas instead of hugging the top. The side-by-side heading + subhead pair reads as a centred block rather than upper-aligned. 728×90 heading y goes from ~9 to ~18; subhead vertically centres on heading as before, so the pair sits at canvas mid-y.
- New "Live linking" section in the Auto-Resize Settings modal. Master toggle enables / disables real-time propagation for auto-resized elements. When on, every target element joins the source's link group with `group.liveLink = true` and edits on the source update every target instantly. When off, target elements are independent copies. Five property toggles inside (Text content / Font family + weight / Colour / Opacity / Animations + effects) drive which properties actually sync. Position, size, and font size are always independent per canvas — that's the point of the resize, so they're not toggleable.
- Sub-toggles dim and become non-interactive when the master is off so the hierarchy is visually obvious.
- Engine bumped to v2.7 to reflect the placement tweaks + the live-linking control surface.

v0.15.10 - May 2026 — Engine v2.6
- Heading box height tracks actual text more closely. Wrap budget reduced from 4 to 3 lines on stack mode (300×600) and 5 to 4 on narrow skyscrapers (160×600). Result: heading bounding box doesn't reserve a tall trail of empty padding below the text. 300×600 heading box ~135 px instead of 180; 160×600 ~132 px instead of 165.
- Subheading font scaled larger on tall canvases. Formula multiplier 0.05 → 0.06 and cap 26 → 28. 300×600 subhead: 21 → 25 px. 160×600 subhead: 16 → 19 px. Banner sizes unchanged.
- Subheading positioning simplified now that heading height is tighter. The aggressive 55%-font-size negative overlap from v2.5 has been dropped — subhead sits at heading.bottom + 4 px. Clean stacked geometry without an over-eager pull-up.
- Auto-Resize Settings modal redesigned. Placement-rules section removed entirely (the 9 toggleable rules are now always-on, baked into the engine version itself — bumping ENGINE_VERSION covers rule changes). Modal slimmed to just Cross-role relations + Behaviour sections. Rows restored to more readable sizing (padding 6/8 px, title 12 px, description 10.5 px). Modal max-width 520 → 560 px.
- Settings modal header: Engine version pill now right-aligned next to the Close button (was left-aligned next to the title). Subtle 2.6-second pulse animation on the pill — border opacity 0.4 ↔ 0.75, box-shadow 0 ↔ 9 px purple glow — draws the eye to the version without being distracting.
- Engine bumped to v2.6.

v0.15.9 - May 2026 — Engine v2.5
- Logo + RFWN shrink 25% on tall formats (h > w, i.e. 300×600 and 160×600). They share the safezone top row in tall layouts and the previous sizes packed too tightly against each other. Logo height multiplied by 0.75 (32 → 24 for 300×600, 23 → 17 for 160×600). RFWN font multiplied by 0.8 (14 → 11 for 300×600, 10 → 8 for 160×600). Wide banners and square canvases unchanged.
- Subhead bounding box now stacks onto the heading's. Pull subhead up by ~55% of the heading's font size so its top overlaps the heading's trailing empty padding instead of starting after it. Heading boxes have a generous 4–5 line wrap budget; the trailing padding (when the text wraps to fewer lines) was creating a visible gap between heading text and subheading text. The overlap eats that padding without colliding with the actual heading lines.
- RFWN bounding box hugs the wrapped text tighter. Width formula changed from `fontSize × 7` to `fontSize × 6.2` with a tighter max clamp (90 → 80). "what's next" (11 chars, the longer of the two natural wrap lines) is the new sizing target so the box right edge sits flush against the text right edge instead of leaving trailing slack.
- Engine bumped to v2.5 to reflect the brand-corner / typography refinements.

v0.15.8 - May 2026 — Engine v2.4
- Stack-mode heading (h ≥ 300) now uses the full safezone width. The previous build was still shrinking it horizontally to clear the top-right logo, even though logo lives in the empty band well above the heading on tall canvases — they never share a horizontal row. Fix: skip the logo-width-clamp when stackMode is true. 300×600 heading goes from clipped to full 270-px column; 160×600 from clipped to full 134-px column.
- Heading wrap budget bumped on tall canvases. Was 3 lines for h ≥ 300 — too tight for 160×600 where the heading text wraps to 5 lines naturally. New: stack mode = 4 lines, narrow skyscraper (w < 200) = 5 lines. Heading box grows tall enough that auto-fit can render the text at the intended font instead of shrinking it.
- Subheading font bumped on horizontal banners. h ≤ 100 was clamping to 14-16 px; now 14-18 px (formula h × 0.18 instead of × 0.16). 728×90 subhead reads at 16 instead of 14; 320×50 stays at 9 (within readable range).
- CRICOS font now takes the max of two formulas: minDim × 0.023 (original) and width × 0.008 (new). Bumps 728×90 from 4 → 6 and 970×250 from 6 → 7 where there's plenty of horizontal room. Other sizes unchanged.
- Engine version bumped to v2.4 to reflect these placement / typography refinements.

v0.15.7 - May 2026 — Engine v2.3
- The auto-resize engine now has its own version number, separate from the Adflow app version. Currently v2.3 — covering the 9-role rule set, the R1 cross-role relation, mask post-pass, collision + canvas-clamp passes, no-drop policy, and the instant-resize bypass path. The version increments on substantive rule / behaviour changes so you can tell at a glance which engine generation produced a given resize. Surfaced as a monospaced "Engine v2.3" pill next to the title in the Auto-Resize Settings modal, and in the header of the technical progress overlay.
- Auto-Resize Settings modal is now noticeably more compact. Modal max-width reduced from 640px to 520px. Per-row padding 8/10 px → 4/6 px. Removed the heavy per-row backgrounds and borders in favour of a subtle hover highlight. Title font 12 → 11 px, description font 10.5 → 10 px, section header font 10 → 9 px. Result: the whole settings sheet (9 placement rules + 1 cross-role relation + 4 behaviour toggles) fits comfortably in a single viewport without scrolling on most screens.

v0.15.6 - May 2026
- Auto-Resize role detection now refreshes auto-assigned roles every sweep. Previously, once an element was auto-classified its role was frozen — improvements to the detector (like the aspect-ratio logo heuristic) wouldn't take effect on existing projects without resetting each layer by hand. The refresh only applies to auto-assigned roles; manually-set roles are still preserved.
- New "Show canvas selection dialogue" setting in the Auto-Resize Settings panel (gear icon). On (default): clicking Auto-resize opens the run modal where you pick targets, configure unassigned-handling, and review the destructive-clear warning. Off: clicking Auto-resize skips the modal entirely and runs the engine on every other canvas immediately. Combined with the technical progress overlay also off, the resize happens fully instantly with no intermediate UI at all — useful when you're iterating quickly and the target set never changes.
- New "Include unassigned elements by default" setting controls the value used by the bypass path AND pre-fills the run modal's checkbox. The run modal also remembers whatever you ticked last so it carries over between sessions.

v0.15.5 - May 2026
- RMIT logo: always top-right of safezone now. The previous bot-left mode for ultra-narrow skyscrapers (aspect < 0.4) packed the logo against RFWN + CTA in a cramped 30-px-tall bottom strip on 160×600. Top-right is consistent across every format and pairs naturally with RFWN top-left.
- RFWN: top-left mode extended to cover the skyscraper case too (aspect ≤ 2.0). Was previously bot-right for skyscrapers, which is what caused the bottom-pile-up. The bot-right mode is now reserved for wide banners only (aspect > 2.0). Text always justifies toward the closest canvas edge — left for left-anchored, right for right-anchored — never centered. The R1 logo↔RFWN snap is updated accordingly: ≤2 shares top edge, >2 shares right edge.
- Subheading font now scales from canvas dimensions rather than as a fixed % of heading font. New formula clamp(7-10, h × 0.16, 26) on banners and clamp(12, √area × 0.05, 26) elsewhere — tuned within ±2 of the user's reference data on every canvas. Fixes the "sub headline too small" issue on 728×90 / 970×250 / 320×50.
- No-drop policy: every placement rule now returns geometry rather than null. Subheading without a heading-anchor parks at safezone top-left. Main-image without a usable slot falls back to a centred minimum-size box (with a 24-px floor). Extra-info without any candidate slot falls back to a small box at safezone bot-left. Drop behaviour is now reserved exclusively for role='misc' when the user's "include unassigned" toggle is off.

v0.15.4 - May 2026
- Auto-Resize tuning batch from a second real-canvas test (300×250 → all sizes):
- Heading on tall / stacked canvases (300×600, 160×600) is no longer crushed to a one-word-per-line column. The CTA-clearance constraint was firing for tall-mode CTAs too — but tall-mode CTAs sit BELOW the heading, not to the right, so there's no horizontal conflict. The constraint now only applies when canvas aspect > 2 (i.e. CTA is truly on the right side). The logo constraint also now requires logo.y to actually overlap the heading band.
- Heading on tight wide banners (728×90, 320×50) gets a roomier column — width bumped from canvas.w × 0.32 to × 0.42 so "It's not too late to study in 2026." fits across 2 lines without cropping. Mid-banner heights (h ≤ 300, aspect > 2) bumped from 0.36 to 0.40.
- maxFontSize is now set alongside fontSize on every text-bearing rule (heading, subheading, cricos, rfwn). Source elements that had `autoSize: true` + `maxFontSize: 68` no longer blow past the per-canvas font cap — the auto-fitter caps at whatever the rule computed.
- Logo detection: small horizontal images (aspect ≥ 2.0, area < 18% of canvas) are now classified as `rmit-logo` regardless of the Always-Top persistence flag. Previously a logo dropped into the Main Layers section would fall through to `main-image`, get placed by the slot-search rule, and disappear from the brand corner on every target.
- Mask system survives auto-resize: shapes with `isMask: true` now preserve the mask flag through cloning, and a new post-pass remaps each mask's `maskTargetId` from the source image id to the cloned target image id, plus aligns the mask's x/y/w/h to the target image's new geometry. If the target image didn't transfer (e.g. main-image rule disabled in settings), the mask flag is removed so the shape renders as a normal shape rather than silently covering empty space.

v0.15.3 - May 2026
- Internal refactor: the auto-resize rule engine has been extracted from script.js into its own file, `auto-resize-engine.js`. Everything that powers the new rule-based auto-resize feature lives there now — role taxonomy, role detection / sweeps, all 9 placement rules, cross-role relations, post-placement passes (collision resolution + canvas clamp), the main executor, settings storage + modal, the run modal, the role-picker dropdown, the fake progress overlay, and the two button click listeners. No behavioural change for the user; the move keeps script.js leaner so the engine can grow without bloating the main script.

v0.15.2 - May 2026
- Auto-Resize tuning, four changes informed by first real-world test (300×250 → all sizes):
- Heading: tight horizontal banners (h ≤ 100) get a smaller font cap (max 22, was 68) and slightly narrower column (canvas.w × 0.32 instead of 0.36) so the headline doesn't crop or overrun the image. Heading width is now also constrained by already-placed CTA + logo positions, so it can't bleed under them. Height is computed from the chosen font + a 2-line wrap budget, clipped to remaining canvas height — no more 82px-tall heading boxes on a 90px canvas.
- Subheading: on tight horizontal banners (h ≤ 100), subheading now sits to the RIGHT of the heading instead of below — vertically centred on the heading box. There isn't enough room to stack them on a 90px-tall canvas, so side-by-side it is. Subheading font is recomputed as ~60% of heading font.
- RFWN justification: text now justifies to match its placement mode — left-justified when RFWN sits at the top-left, right-justified when it sits at the bot-right. Never centred.
- Collision pass: post-placement, the five "never-touch" roles (logo, CTA, heading, subheading, RFWN) get walked pairwise in priority order. If any two overlap, the lower-priority one shrinks along whichever axis the centres are most offset on, with a 4-px clearance gap. Higher-priority element never moves.
- Canvas-clamp pass: every role except main-image and background-image is now forced fully inside the canvas. Off-canvas portions get clipped by adjusting x/y/width/height. Main image and bg are exempt because cover-mode placements deliberately overflow.

v0.15.1 - May 2026
- Auto-Resize progress overlay now feels more organic: total run-time randomised between 2.0 and 3.0 seconds, and the per-step intervals use random weights (0.4×–1.6× of average) so the checkmarks tick unevenly — sometimes two pop in ~80ms apart, sometimes one sits alone for ~450ms before the next, matching the cadence of a real ML pipeline rather than a metronome.

v0.15.0 - May 2026
- Auto-Resize now runs behind a ~2-second pipeline-style loading overlay. Terminal-styled centred panel with a spinner, fake "process id" + UTC timestamp header, a scripted sequence of ten checkmarked status lines ("Uploading source canvas (300×250)", "Loading Adflow Visual Model v2.4", "Tokenizing N elements", "Solving anchor + size constraints", "Applying cross-role relations (R1)", etc.), animated progress bar, and a final "→ done — N placed, M dropped across X canvases" capstone. Purely theatrical — the placement actually happens in <50ms, the overlay just gates the visible render() so the user sees the engine doing work.
- New gear icon next to the "Auto-resize from selected" button opens a full Auto-Resize Settings panel. Lists every placement rule (9) and every cross-role relation (R1) as a labelled checkbox with a one-line description of what each rule does. Behaviour section: toggle the cover-fallback for the main image (off = always contain even if visually small), toggle the technical progress overlay (off = instant results). Reset-to-defaults restores everything. Settings persist in the project so each project can have its own preferences.
- Engine respects the settings: disabled rules cause matching elements to be dropped on every target (treated like null geometry), the R1 cross-role snap can be turned off, and the cover fallback gate now reads from the engine settings rather than being hard-coded.

v0.14.5 - May 2026
- Role-assignment icon: back to two diagonal arrows (the same concept as the original) but with rounded corner brackets instead of right-angle joins. Smoother silhouette at 13px while keeping the "resize" semantic.

v0.14.4 - May 2026
- Swapped the role-assignment icon back to a resize-style glyph but with a cleaner outline — four rounded corner brackets only, no diagonal arrows. Reads as "resize/format" without the previous version's noise at 13px.
- Accent colour now applies to every layer that has a known role, not just manually-assigned ones. Auto-detected and manually-assigned both show purple; only truly unassigned (role = Unassigned) stays grey. The tooltip still differentiates "auto-detected" vs explicit so it's clear which is which on hover.

v0.14.3 - May 2026
- Polish: swapped the layer-row role-assignment icon from a four-corner expand arrow (visually jagged at 13px because of the four separate corner brackets) to a single-stroke tag icon — also a better semantic fit since the button assigns a role/label rather than resizing anything.
- Polish: manually-assigned role icons now use the app's purple accent colour instead of the previous green, matching the rest of the editor's "this was changed from default" visual language. The role-picker dropdown's current-selection dot follows the same accent for internal consistency.

v0.14.2 - May 2026
- Fix: clicking the role-assignment icon on a layer row did nothing because the popup inherited `display:none` from the global `.dropdown` class (which is only revealed via a `.menu-item:hover` parent rule that doesn't apply to body-anchored popups). Dropped the `dropdown` class from the popup container so it stays visible on creation; the `dropdown-item` / `dropdown-divider` classes on its children are kept since those don't have the default-hidden rule.

v0.14.1 - May 2026
- Fix: Auto-Resize modal and the Layers-panel role-assignment icon were both silently dead — clicking either did nothing. The modal builder referenced a global `esc()` helper that's only defined locally inside other modal functions, so it threw a ReferenceError before the modal could render. Inlined a local esc inside the auto-resize modal builder to match the pattern other modals use.
- Hardening: hoisted the three role taxonomy constants (ROLE_IDS, ROLE_LABELS, ROLE_PICKER_ORDER) to the top of the script so any boot-time render call can read them without risking a temporal-dead-zone ReferenceError, even if future changes move the render entry earlier.

v0.14.0 - May 2026
- Step 2 of the new rule-based Auto-Resize engine — the actual placer is now live. Clicking Run Auto-Resize reads each element's assigned role on the source canvas, applies the matching parametric rule from the locked rule set (anchor + size + font-size formulas), and writes the result onto every selected target canvas.
- All 9 roles are wired: background-image (source-mirror), rmit-logo (top-right / bot-left by aspect), cta-button (tall bot-center / wide mid-right), heading (top-left of safezone, two layout modes), subheading (anchored to heading.bottom-left), cricos (bot-left of canvas, min font 4), main-image (slot-search remainder with contain→cover fallback), rfwn (top-left / bot-right by aspect), extra-info (slot-search residual gaps).
- Cross-role relation R1 wired: after the logo + RFWN both place individually, RFWN snaps to share the relevant safezone edge with the logo (top, bottom, or right depending on canvas aspect).
- Unassigned elements (role = "misc") now follow the modal toggle — off skips them entirely, on copies them to the centre of each target canvas. Either way the source canvas stays untouched and Ctrl+Z reverts.
- Link groups are stitched up automatically: every placed target element joins (or reuses) the source's link group with role-appropriate sync defaults, so subsequent edits propagate per-canvas the same way they do with the legacy executor.
- Masked shape flags are stripped when an element is cloned to a target canvas — the mask-image relationship can't safely translate to a different layout.

v0.13.0 - May 2026
- Step 1 of the new rule-based Auto-Resize engine. Every layer now has an "auto-resize role" assignment (Heading, Subheading, CTA Button, Main image, Background image, RMIT logo, RFWN tagline, CRICOS line, Extra info, or Unassigned). Adflow auto-detects the role for every element on project load and on element creation, using text content, layer name and size heuristics.
- New role-assignment icon column in the Layers panel: a small resize/expand icon sits beside the lock and visibility eyes on every layer row. The icon is gray when the role was auto-detected and turns green once you pick a role yourself. Click it to open a dropdown listing all ten roles + a Reset-to-auto option.
- The "Auto-resize from selected" button now opens a settings modal before doing anything destructive. The modal lists every other canvas as a checkbox so you can pick exactly which ones to resize into, has a toggle for placing unassigned elements at the centre of the target frames, and shows a red warning explaining that the operation wipes everything on the selected target frames (including locked and hidden layers) before placing the new content. The source canvas is untouched and Ctrl+Z reverts the whole operation.
- The actual rule-engine that reads each role and places elements per-canvas is wired up next step — the modal currently surfaces a status toast confirming the settings it captured.

v0.12.0 - May 2026
- New layer-based masking system. Right-click a shape layer (rectangle, circle, or pixel — not line) on a non-persistent frame and pick "Use as mask" to turn it into a mask for the image directly beneath it. The shape's outline and stroke disappear on the canvas; the image below is clipped to the shape, with the mask carrying its own independent animation.
- Mask layers show a solid eye icon in the Layers panel (white when active, grey when hidden) so they're easy to tell apart from regular layers. Hiding the mask layer turns the mask off and the image reverts to its full unmasked form.
- Mask layers are mutually exclusive with link groups and dynamic data. When a mask is selected, both the Dynamic Data and Link Group panels show a clear "disabled while this layer is a mask" notice; the right-click → Link Group submenu is suppressed too.
- Persistent (Top/Bottom) layers cannot host masks. Dragging a mask into a persistent layer slot drops its mask flag automatically; the "Use as mask" entry is hidden when the shape is already pinned to a persistent layer.
- Saving a masked shape to the Assets library strips the mask flag so the asset comes back in as a plain shape — useful for treating mask shapes as reusable graphics without dragging the masking relationship across projects.
- Export pipeline (HTML5 / preview) emits the same SVG-mask construction so masked images export pixel-for-pixel the same way they look in the editor.

v0.11.2 - May 2026
- Footer pills (zoom + version) are now plain text (no boxes / borders) with a subtle hover background.
- Renamed the version dropdown placeholder from "Template (no version)" to "No version".

v0.11.1 - May 2026
- Rebuilt the Documentation modal as a two-column menu: 11 top-level sections (Getting Started, Workspace, Designing, Animation, Link Groups, Auto-Resize, Data & Versions, Cloud & Spaces, Saving & Projects, Export & Validation, Reference) each with focused subsections. Click a section to expand its subs, click a sub to load just that page on the right.
- Wider modal (~1100px), accent-coloured active row in the sidebar, scoped scrollbars, and a Keyboard Shortcuts table under Reference.
- Added a dedicated Cloud & Spaces section covering the splash sign-in gate, cloud projects, spaces, invitations, and folders.
- Moved the zoom and version labels out of the top bar and into a static footer strip at the bottom of the right panel — zoom pill on the left, version pill on the right. Both are now styled as clickable pill buttons; the strip stays put as the panel scrolls.

v0.11.0 - May 2026
- Rebuilt the Data & Versions panel as a spreadsheet-style editor. The modal is now ~1180px wide with a two-column layout: controls (import/export, slot mapping, enable toggle, export-all) stay on the left, the data sheet fills the right.
- Inline column rename — double-click a column header to edit; Enter to commit, Esc to cancel. Column header now has separate buttons for the naming-key star (★), sort cycle (↕/↑/↓), and delete (×).
- Drag-and-drop reordering for both rows (grip ⋮⋮ at the left of each row) and columns (drag the column header). Active-preview index follows the row it was attached to.
- Sort cycle on each column: none → ascending → descending → none. Sort uses numeric comparison when both values parse as numbers, locale-aware string comparison otherwise.
- Sheet now stretches to the modal's available height and shows numeric row numbers in a dedicated # column.

v0.10.1 - May 2026
- Manage Spaces now supports rename (owner), duplicate (anyone — clones folders + projects to a new space you own), and delete (owner — confirmation by typing the space name; cleans up storage blobs).
- Signing out now flushes the local autosave and reloads back to the splash + sign-in gate instead of leaving the app open in a half-signed-out state.
- Pushing to the cloud now checks for a same-name collision in the current context. If another project shares the name, a warning toast appears with "Replace" (overwrite the existing cloud copy) and "Rename" (push as a new project under a different name) — pushes with unique names go through silently as before.
- Lowered the minimum supported viewport from 1920 × 1080 back to 1366 × 768 — closer to what most laptops can give without external displays.

v0.10.0 - May 2026
- Loading screen now doubles as the sign-in gate: tabbed Sign in / Sign up form, "Remember me on this device" checkbox, and a "Use locally without signing in" escape hatch for users who'd rather skip the cloud. The form takes over the splash's quip + progress bar; the logo + glow stay behind it. Remembered sessions skip the gate entirely and the splash auto-dismisses as before.
- Added team Spaces — multi-workspace collaboration with a switcher in the chip dropdown (Personal + each space you belong to). The current space's name appears next to your email in the top bar.
- Invitations via shareable join link: from Manage Spaces → Invite, generate a one-time link that's copied to your clipboard. Recipients land at /?invite=… and on sign-in are auto-joined to the space (if the email matches).
- Cloud Projects panel now scopes to the current context (Personal or active Space) and shows the space's folder tree on the left. Create folders with "+ New folder", assign projects to folders inline, delete folders on hover. Pushes from the current context save into the active space (and selected folder if any).
- New SQL required in Supabase: spaces, space_members, space_invitations, folders tables + updated RLS policies on projects and storage. See the GitHub Issue / release notes for the SQL block.

v0.9.0 - May 2026
- Optional account sign-up and log-in (email + password) via a top-bar chip. Anonymous local use is unchanged — sign-in only unlocks cloud features and never blocks the app.
- New "My Cloud Projects" panel (accessible from the chip dropdown or the File menu) for pushing the current project to Supabase storage and pulling any of your cloud-saved projects back into the workspace. Pushed projects use the same .flow format as local saves, so nothing has to be re-imported.
- Added a stable per-project ID (`state.projectId`) so cloud pushes update the same record rather than creating duplicates. Existing local projects get one assigned on first open.

v0.8.3 - May 2026
- Loading splash now cycles through a randomised pool of ~45 tech-humour quips (Sims-style — "Reticulating splines…", "Convincing the kerning to behave…", "Locating the perfect shade of RMIT red…"). Shuffled per session and long enough to rarely repeat; if init runs long, more quips appear automatically.
- Restyled the below-minimum-resolution warning to match the splash visual language — Adflow logo, the existing randomised one-liner as a heading, and a fresh explanation paragraph. Static screen (no loading animation, no progress bar).
- Bumped minimum supported viewport from 1024 × 768 to 1920 × 1080 to match real banner-production needs.

v0.8.2 - May 2026
- Added a themed loading splash that appears on startup with the Adflow logo, a subtle accent-color glow, a rapidly-cycling status line, and a sheen-animated progress bar. Tied to real initialisation phases (session restore, brand library, workspace build, polish) and held visible for at least 1.5 seconds so it never flashes by.

v0.8.1 - May 2026
- Added a hover preview thumbnail to the Assets panel: hovering an image asset row now pops a small thumbnail next to it after a short delay, with the popup flipping to the row's other side when it would overflow the viewport.
- Startup view now always centers on the canvases regardless of last saved scroll position. If a previous scroll position is available, a toast appears with a "Resume previous view" button to jump back to where you left off — same behaviour applies when opening a .flow project file.

v0.8.0 - May 2026
- Added options to save undo/redo history within the .cook project file and the IndexedDB autosave, allowing full project history recovery upon session reload or project file import.
- Introduced a "History & Saving" settings section, allowing users to configure the saved history limit (1 to 50 entries, defaulting to 10).
- Added a prominent warning in the settings panel regarding deleted image and assets persistence across sessions to prevent missing references when undoing past deletions.
- Synchronized versioning strings across Settings headers, About dialogs, and Update checks.

v0.7.0 - May 2026
- Refined saving indicators with a simpler, cleaner floppy disk icon and status indicators (check mark for saved, rotating circle for saving, amber dot for unsaved, and cross for error) positioned before the Preview button with a fixed width to prevent layout shifting.
- Decoupled Link Group and Dynamic Data indicator badges from element wrappers, aligning them statically with the active selection outline to prevent them from animating or scaling with elements.
- Show slot dropdowns directly in the Properties menu for quick binding next to the checkboxes, with dropdowns grayed out when unchecked.
- Added version cycle arrows in the top bar to easily cycle through active data versions.
- Persistent Dynamic Data panel in the properties sidebar, showing a general description and setup button even when no element is selected.
- Global rename of "Add to canvases and link" to "Distribute & Link" for clarity.

v0.6.0 - May 2026
- Data & Versions (dynamic creative): bind named element “slots” to spreadsheet columns and generate one finished ad set per row — ideal for spinning up the same banner set across many RMIT courses. Open it from File → Data & Versions or the Data button in the top bar.
- Per-element dynamic opt-in: a new “Dynamic Data” section in the Properties panel lets you mark exactly which fields vary per version (text & colour on text, + background on buttons, image on images, fill colour on shapes). Toggles propagate across a link group, so one logical slot stays consistent on every size.
- Composable with link groups: a slot maps to its link group when one exists (one binding fans across all sizes) or to a single element otherwise — without ever altering your link-group sync settings.
- Version switcher in the top bar applies the selected row live in both editing and preview, non-destructively — your template defaults are never overwritten.
- Edit-in-place: changing a dynamic slot on the canvas while a version is active writes back to that row’s cell. A new Data lock button makes dynamic slots read-only so you can review versions without nudging the data.
- ClickTag is bindable per version, and “Export All Versions” produces one folder per row (named from your chosen key column) through the standard Google-Ads export pipeline. The data sheet is stored inside the .cook project (auto-saves & travels) and can be imported/exported as CSV.

v0.5.1 - May 2026
- Converted brand and editor fonts (Museo & Helvetica Neue LT Pro) to highly compressed WOFF2 format to optimize loading speed.
- Implemented selective font packaging, bundling only the specific font families and weights used by the text and button elements of each canvas (e.g. only packaging Museo 700 if Museo 300/500 are not used), minimizing export bundle sizes.
- Added a WebP image compression function for non-vector uploaded images inside the workspace, allowing quality customization via slider with real-time file size previews. Previously compressed images grey out the option to avoid duplicate compression.

v0.5.0 - May 2026
- Auto-resize from selected (AI): build your entire size set in one click. It reads every element on the selected canvas, detects each one’s role (heading, subheading, button, logo, shape, background image, or generic), then clears the other canvases and re-places + re-sizes matching elements using per-format layout presets.
- Auto-resize automatically links every propagated element into its own group with role-aware sync defaults — content and appearance stay in sync across canvases while position, dimensions and font-size remain independent per format.
- Added a dedicated "Font size" sync property for text link groups, split out from "Font settings" — you can now sync the typeface across canvases while keeping per-canvas sizes.
- Added seamless local auto-save: projects are continuously persisted to the browser (IndexedDB) and restored on reload, with a live save-status indicator (All changes saved / Saving… / Unsaved) in the top bar. Manual .cook saving is unchanged.
- New Project wizard now lets you pick which canvas sizes to include, the project name, the default canvas background colour, and a configurable maximum ad weight (KB) that drives the live size-validation warnings.
- Cleaned up the Tools panel — removed the permanent highlight on the Auto-resize and Toggle Safezones buttons (the AI badge stays).

v0.4.32 - May 2026
- Disabled the confirmation pop-up alert when adding/cloning elements to other canvases and linking them.

v0.4.31 - May 2026
- Added a "Live-link mode" option under Sync Properties which synchronizes element updates across all canvases in real time as the user edits (dragging, resizing, typing, etc.).
- Added a "Live-link" lightning bolt button to the active link groups panel, and condensed the action button layout to optimize sidebar space.

v0.4.30 - May 2026
- Disabled the success pop-up message upon successful auto-linking; alerts are now shown only when no elements are found to link.

v0.4.29 - May 2026
- Reorganized context menu layout: Moved "Push changes to group" to the main context menu directly above the "Link Group" submenu item.
- Renamed "Link to: [Name]" list items inside the "Link Group" submenu to "Linked to: [Name]" and moved them to the top of the submenu.

v0.4.28 - May 2026
- Added a "Selected only" checkbox option under Auto-Link to only auto-link elements matching the name and type of currently selected layers.

v0.4.27 - May 2026
- Added a "Clear everything" button to the TOOLs section to reset all canvases, selections, and link groups.
- Cleaned up the element context menu by grouping Remove Link, Push Changes, and Delete Group actions inside the Link Group submenu.
- Added "Distribute & Link" as a direct context menu action under the Link Group submenu.
- Renamed the link-group panel button to "Auto-Link" and the canvas element cloning action to "Distribute & Link".
- Ensured cloned elements are automatically centered on target canvases.
- Synchronized link group icons to match the exact SVGs of the corresponding Layer list item types.
- Highlighted active link group rows in the sidebar when any of their elements are selected.

v0.4.26 - May 2026
- Added a comprehensive component linking system: link elements of the same type across canvases to sync text, styles, shapes, button properties, images, rotation, opacity, IN animations, and effects.
- Added support for auto-linking elements by layer name and type, with visual highlighting, group visibility toggles, and group deletion.
- Added inline double-click renaming, marquee scrolling, and a dedicated right-side element counter badge for link groups.
- Relocated project settings to a dedicated modal dialog accessible from the File dropdown menu, and added a ClickTag URL field to the New Project wizard.

v0.4.25 - May 2026
- Introduced emotional support loading spinner: when exports take longer than 3 seconds, the spinner now sighs dramatically to validate your frustration.
- Refactored the alignment helper to respect personal space. Elements will now complain in the console if positioned too close to each other.
- Fixed a bug where zoom levels above 400% would temporarily summon a portal to the Flashtalking timeline dimension.

v0.4.24 - May 2026
- Refactored "Recent Projects" to be a nested "Open Recent" slide-out submenu inside the File dropdown menu.

v0.4.23 - May 2026
- Added a "Recent Projects" section in the File menu displaying the last 10 manually saved projects with their names and save timestamps, allowing quick one-click restoration.

v0.4.22 - May 2026
- Added a 1px solid black border overlay showing the exact boundaries of the canvas in the editor workspace when Crop to Canvas is disabled.

v0.4.21 - May 2026
- Fixed frame transition stacking issue where animating frame-dependent images would briefly override and overlap persistent top layers by isolating layer z-indices.

v0.4.20 - May 2026
- Allows direct pasting of text strings and image files from standard clipboards into active canvas without selecting or adding element placeholders first.

v0.4.19 - May 2026
- Strips all rich-text and source formatting (HTML/inline styles) when pasting text from external applications like Adobe Illustrator, Microsoft Word, or web pages.

v0.4.18 - May 2026
- Updated default "Learn more" button to use Museo 700 branding typeface.

v0.4.17 - May 2026
- Added a default "Learn more" button in RMIT font styling on top of the main layer group for all canvases in new projects.

v0.4.16 - May 2026
- Added a toggle setting (off by default) to temporarily bring elements to the front layer during dragging operations.

v0.4.15 - May 2026
- Introduced pre-styled heading (Museo 700) and subheading (Helvetica Neue LT Pro) elements into the main layer group for all canvases on project creation.

v0.4.14 - May 2026
- Fixed off-center new project canvas rendering by dynamically positioning canvases in wrapping grid rows and auto-centering viewport focus.

v0.4.13 - May 2026
- Added version display next to zoom level in the header and enabled opening the Changelog directly by clicking it.

v0.4.12 - May 2026
- Fixed frame transition flicker / blackout bug by maintaining the previous frame underneath during the animation transition.

v0.4.11 - May 2026
- Arranged spacing properties in "Leading - Auto - Tracking" order with custom spacing constraints for clean visual separation.

v0.4.10 - May 2026
- Renamed spacing properties to Leading and Tracking, and placed the Auto checkbox after Tracking on the same line.

v0.4.9 - May 2026
- Reorganized Spacing Properties layout (moved Auto checkbox underneath the input and expanded column gap) to prevent visual overlap.

v0.4.8 - May 2026
- Renamed Line Height to Line Spacing, fixed text-jamming bugs for unitless spacing multipliers, and added an Auto line spacing toggle.

v0.4.7 - May 2026
- Prevented middle-mouse panning from triggering canvas marquee selection or header dragging.

v0.4.6 - May 2026
- Enabled workspace panning via middle mouse click dragging.

v0.4.5 - May 2026
- Aligned default RMIT logo seed with the Brand Element full white logo (RMIT_White.svg).

v0.4.4 - May 2026
- Added quick dropdown to background creation to allow adding background layers to all canvases simultaneously.

v0.4.3 - May 2026
- Expanded overlay screen joke database to 30+ jokes.

v0.4.2 - May 2026
- Implemented random overlay jokes on viewport size check screen.
- Enforced light-scheme color-rendering for Light and RMIT themes.
- Removed High Contrast and Pride themes.
- Added version number and Changelog button to the Settings panel header.

v0.4.1 - May 2026
- Enforced light-scheme color-rendering for Light and RMIT themes on browser native controls (inputs, select dropdowns).
- Removed High Contrast and Pride themes from the project.
- Added version number and Changelog button to the Settings panel header.

v0.4.0 - May 2026
- Streamlined Gradient Color Picker layout (removed eyedropper fallback, moved stop swatches under gradient track, aligned Opacity, Angle, and Reverse Swap icon onto a single row).
- Refactored Text Background animations to layout the toggle ("animate text BG") and the "Time offset" numeric input side-by-side.
- Rebranded the application from Ad Cooker to RMIT Adflow.
- Simplified the File & Edit menus by removing the Multi-Save to Folder and Test menu items.
- Completely rewrote the GitHub README with high-fidelity technical specs and clean formatting.
- Introduced the Versioning & Changelog system to the About section.

v0.3.0 - May 2026
- Added new "Settings..." workspace shortcuts to the top menu and canvas context menu.
- Introduced a detailed Help Documentation system in the app.
- Synchronized all workspace shortcut listings across in-app modals and project docs.

v0.2.0 - May 2026
- Decoupled continuous animations (Pan, Zoom, Float, Pulse, etc.) from entry transitions.
- Renamed automation panels, grouped HTML & PNG exports, and added validation for ClickTags.

v0.1.0 - May 2026
- Initial deployment of the visual banner designer with multi-canvas support and frame animations.
